# Horno San Lorenzo — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship a polished, fully static Astro 5 site for Horno San Lorenzo (pitch demo) with a WhatsApp-based ordering flow, deployable to Vercel.

**Architecture:** Astro 5 static output (`output: 'static'`), React islands only where state is needed (cart badge, add-to-cart, cart page, filters, toast, contact form, cookie banner), Tailwind v4 via `@tailwindcss/vite` (no config file, `@theme` block in CSS), Fraunces + Inter self-hosted via `@fontsource-variable/*`, content modeled as Astro Content Collections (Zod schemas).

**Tech Stack:** Astro 5, React 19, Tailwind v4, TypeScript strict, motion, zod, @vercel/analytics, pnpm 10, Node 24, Vercel Hobby.

**Spec:** [`docs/superpowers/specs/2026-05-19-hornosanlorenzo-demo-design.md`](../specs/2026-05-19-hornosanlorenzo-demo-design.md)

---

## File map (created by this plan)

```
hornosanlorenzo/
├── .env.example
├── .gitignore                          (already exists, will be appended)
├── .nvmrc
├── README.md
├── package.json
├── pnpm-lock.yaml                      (generated by pnpm install)
├── tsconfig.json
├── astro.config.mjs
├── public/
│   ├── favicon.svg
│   ├── robots.txt
│   ├── og-image.jpg
│   └── images/{productos,noticias,tiendas,hero}/*.jpg
└── src/
    ├── content.config.ts               (Zod schemas)
    ├── content/
    │   ├── products/*.md               (~20 products)
    │   └── noticias/*.md               (~5 noticias)
    ├── data/
    │   ├── site.ts
    │   ├── stores.ts
    │   ├── services.ts
    │   ├── categories.ts
    │   └── tradicion.ts
    ├── lib/
    │   ├── cart.ts
    │   ├── whatsapp.ts
    │   └── format.ts
    ├── hooks/useCart.ts
    ├── styles/global.css
    ├── layouts/
    │   ├── BaseLayout.astro
    │   ├── MarketingLayout.astro
    │   └── LegalLayout.astro
    ├── components/
    │   ├── SEO.astro
    │   ├── Header.astro
    │   ├── Footer.astro
    │   ├── Hero.astro
    │   ├── SectionLabel.astro
    │   ├── PromoBanner.astro
    │   ├── MapEmbed.astro
    │   ├── ProductCard.astro
    │   ├── StoreCard.astro
    │   ├── ServiceCard.astro
    │   ├── NewsCard.astro
    │   └── SkipLink.astro
    ├── islands/
    │   ├── CartBadge.tsx
    │   ├── AddToCart.tsx
    │   ├── CategoryFilter.tsx
    │   ├── CartPage.tsx
    │   ├── ToastHost.tsx
    │   ├── ContactForm.tsx
    │   └── CookieBanner.tsx
    └── pages/
        ├── index.astro
        ├── catalogo/index.astro
        ├── catalogo/[slug].astro
        ├── carrito.astro
        ├── servicios.astro
        ├── tradicion-y-calidad.astro
        ├── noticias/index.astro
        ├── noticias/[slug].astro
        ├── tiendas.astro
        ├── contacto.astro
        └── legal/{aviso-legal,privacidad,cookies}.astro
```

---

## Phase 1 — Bootstrap

### Task 1: Project files (package.json, tsconfig, .nvmrc, .gitignore)

**Files:**

- Create: `package.json`
- Create: `tsconfig.json`
- Create: `.nvmrc`
- Create: `.env.example`
- Modify: `.gitignore` (append)

- [ ] **Step 1: Write `package.json`**

```json
{
  "name": "hornosanlorenzo-demo",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "engines": { "node": ">=20" },
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/react": "^4.4.0",
    "@astrojs/sitemap": "^3.6.0",
    "@fontsource-variable/fraunces": "^5.2.10",
    "@fontsource-variable/inter": "^5.2.10",
    "@tailwindcss/vite": "^4.1.13",
    "@vercel/analytics": "^1.5.0",
    "astro": "^5.14.0",
    "motion": "^11.18.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "tailwindcss": "^4.1.13",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "typescript": "^5.8.0"
  }
}
```

- [ ] **Step 2: Write `tsconfig.json`**

```json
{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  "exclude": ["dist"],
  "compilerOptions": {
    "jsx": "react-jsx",
    "jsxImportSource": "react",
    "baseUrl": ".",
    "paths": {
      "~/*": ["src/*"]
    }
  }
}
```

- [ ] **Step 3: Write `.nvmrc`**

```
24
```

- [ ] **Step 4: Write `.env.example`**

```
PUBLIC_WHATSAPP_NUMBER=34666123456
PUBLIC_SITE_URL=https://hornosanlorenzo-demo.vercel.app
```

- [ ] **Step 5: Append to `.gitignore`**

Read current `.gitignore` first (already contains `.superpowers/`), then add:

```
node_modules/
dist/
.astro/
.vercel/
.env
.env.local
.env.*.local
.DS_Store
*.log
```

- [ ] **Step 6: Install dependencies**

Run: `pnpm install`
Expected: `pnpm-lock.yaml` generated, `node_modules/` populated, no errors.

- [ ] **Step 7: Commit**

```bash
git add package.json pnpm-lock.yaml tsconfig.json .nvmrc .env.example .gitignore
git commit -m "chore: bootstrap project (package.json, tsconfig, env example)"
```

---

### Task 2: Astro config + minimal entry

**Files:**

- Create: `astro.config.mjs`
- Create: `src/pages/index.astro` (placeholder — replaced in Task 19)
- Create: `src/env.d.ts`

- [ ] **Step 1: Write `astro.config.mjs`**

```js
import { defineConfig } from "astro/config";
import react from "@astrojs/react";
import sitemap from "@astrojs/sitemap";
import tailwindcss from "@tailwindcss/vite";

const SITE =
  process.env.PUBLIC_SITE_URL ?? "https://hornosanlorenzo-demo.vercel.app";

export default defineConfig({
  site: SITE,
  output: "static",
  trailingSlash: "never",
  integrations: [react(), sitemap()],
  vite: {
    plugins: [tailwindcss()],
  },
  build: {
    inlineStylesheets: "auto",
  },
  prefetch: {
    prefetchAll: true,
    defaultStrategy: "hover",
  },
});
```

- [ ] **Step 2: Write `src/env.d.ts`**

```ts
/// <reference path="../.astro/types.d.ts" />

interface ImportMetaEnv {
  readonly PUBLIC_WHATSAPP_NUMBER: string;
  readonly PUBLIC_SITE_URL: string;
}

interface ImportMeta {
  readonly env: ImportMetaEnv;
}
```

- [ ] **Step 3: Write placeholder home `src/pages/index.astro`**

```astro
---
---
<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <title>Horno San Lorenzo — bootstrap</title>
  </head>
  <body>
    <p>Bootstrap OK. Replaced in Task 19.</p>
  </body>
</html>
```

- [ ] **Step 4: Verify dev server starts**

Run: `pnpm dev`
Expected: Astro starts at `http://localhost:4321`, opens to the bootstrap page without errors. Stop with Ctrl-C.

- [ ] **Step 5: Verify production build**

Run: `pnpm build`
Expected: `dist/index.html` created, no errors.

- [ ] **Step 6: Commit**

```bash
git add astro.config.mjs src/env.d.ts src/pages/index.astro
git commit -m "chore: astro config + bootstrap entry"
```

---

## Phase 2 — Visual foundation

### Task 3: Global CSS with Tailwind v4 tokens

**Files:**

- Create: `src/styles/global.css`

- [ ] **Step 1: Write `src/styles/global.css`**

```css
@import "tailwindcss";

@import "@fontsource-variable/fraunces/index.css";
@import "@fontsource-variable/fraunces/wght-italic.css";
@import "@fontsource-variable/inter/index.css";

@theme {
  --color-cream: #f4ecdf;
  --color-paper: #fffdf8;
  --color-ink: #2b1d12;
  --color-ink-muted: #6b4f3a;
  --color-accent: #a07551;
  --color-accent-dark: #7a5a3e;
  --color-hot: #c9242a;
  --color-line: #e6dccb;
  --color-wa: #25d366;
  --color-success: #5a7d3c;

  --font-display: "Fraunces Variable", Georgia, serif;
  --font-body:
    "Inter Variable", system-ui, -apple-system, "Segoe UI", sans-serif;

  --radius-card: 12px;
  --radius-pill: 9999px;

  --shadow-card:
    0 1px 2px rgba(43, 29, 18, 0.04), 0 8px 24px rgba(43, 29, 18, 0.06);
  --shadow-toast: 0 10px 30px rgba(43, 29, 18, 0.18);
}

:root {
  color-scheme: light;
}

html {
  font-family: var(--font-body);
  color: var(--color-ink);
  background: var(--color-paper);
  -webkit-font-smoothing: antialiased;
  text-rendering: optimizeLegibility;
}

body {
  margin: 0;
  min-height: 100dvh;
}

h1,
h2,
h3,
.display {
  font-family: var(--font-display);
  letter-spacing: -0.01em;
}

a {
  color: inherit;
  text-decoration: none;
}
a:hover {
  color: var(--color-accent-dark);
}

img,
picture,
svg,
video {
  max-width: 100%;
  display: block;
}

button {
  font: inherit;
}

.skip-link {
  position: absolute;
  left: -9999px;
  top: 0;
  background: var(--color-ink);
  color: var(--color-cream);
  padding: 10px 16px;
  z-index: 100;
}
.skip-link:focus {
  left: 8px;
  top: 8px;
}

@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.001ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.001ms !important;
    scroll-behavior: auto !important;
  }
}

::view-transition-old(root),
::view-transition-new(root) {
  animation-duration: 0.25s;
}
```

- [ ] **Step 2: Verify CSS compiles**

Add import in `src/pages/index.astro` temporarily (or wait until BaseLayout). For now, just run:

Run: `pnpm build`
Expected: no errors. `dist/` contains the CSS.

- [ ] **Step 3: Commit**

```bash
git add src/styles/global.css
git commit -m "feat(style): warm-artisan tokens, fonts, base CSS"
```

---

## Phase 3 — Data + content

### Task 4: Site, stores, services, categories data

**Files:**

- Create: `src/data/site.ts`
- Create: `src/data/stores.ts`
- Create: `src/data/services.ts`
- Create: `src/data/categories.ts`
- Create: `src/data/tradicion.ts`

- [ ] **Step 1: Write `src/data/site.ts`**

```ts
export const site = {
  name: "Horno San Lorenzo",
  short: "HSL",
  tagline: "Pan de pueblo, hecho a mano. Desde 1986.",
  founded: 1986,
  url: import.meta.env.PUBLIC_SITE_URL,
  whatsapp: import.meta.env.PUBLIC_WHATSAPP_NUMBER,
  social: {
    instagram: "https://instagram.com/hornosanlorenzo_1986",
    facebook: "https://facebook.com/hornosanlorenzo",
  },
  email: "info@hornosanlorenzo.com",
  ogImage: "/og-image.jpg",
  noteForDemo:
    "Demo en presentación — fotos representativas. Los productos, tiendas y datos de contacto son reales.",
} as const;

export type Site = typeof site;
```

- [ ] **Step 2: Write `src/data/stores.ts`**

```ts
export type StoreId = "alcobendas" | "pozuelo";

export type Store = {
  id: StoreId;
  name: string;
  shortName: string;
  address: string;
  city: string;
  postal: string;
  phone: string;
  phoneDisplay: string;
  email: string;
  hoursText: string;
  openDays: number[]; // 0=Sun ... 6=Sat
  mapsEmbed: string;
  mapsLink: string;
  coords: { lat: number; lng: number };
  image: string;
  imageAlt: string;
};

export const stores: readonly Store[] = [
  {
    id: "alcobendas",
    name: "Horno San Lorenzo — Central",
    shortName: "Alcobendas",
    address: "C/ Valgrande 21, Nave 2-k",
    city: "Alcobendas",
    postal: "28108",
    phone: "+34916613932",
    phoneDisplay: "916 613 932",
    email: "info@hornosanlorenzo.com",
    hoursText: "Lun–Sáb 7:00–14:00 · Domingos cerrado",
    openDays: [1, 2, 3, 4, 5, 6],
    mapsEmbed:
      "https://www.google.com/maps?q=C%2FValgrande+21+Alcobendas&output=embed",
    mapsLink: "https://www.google.com/maps?q=C%2FValgrande+21+Alcobendas",
    coords: { lat: 40.5468, lng: -3.6394 },
    image: "/images/tiendas/alcobendas.jpg",
    imageAlt: "Fachada del obrador central en Alcobendas",
  },
  {
    id: "pozuelo",
    name: "Tienda Pozuelo",
    shortName: "Pozuelo",
    address: "Avda. Europa 28, entrada por C/ América",
    city: "Pozuelo de Alarcón",
    postal: "28223",
    phone: "+34916059056",
    phoneDisplay: "91 605 90 56",
    email: "pasteleriapozuelo@hornosanlorenzo.com",
    hoursText: "Lun–Dom 8:00–14:30 y 17:00–20:30",
    openDays: [0, 1, 2, 3, 4, 5, 6],
    mapsEmbed:
      "https://www.google.com/maps?q=Avenida+Europa+28+Pozuelo+de+Alarcon&output=embed",
    mapsLink:
      "https://www.google.com/maps?q=Avenida+Europa+28+Pozuelo+de+Alarcon",
    coords: { lat: 40.4378, lng: -3.809 },
    image: "/images/tiendas/pozuelo.jpg",
    imageAlt: "Tienda de Pozuelo, escaparate con bollería y pan",
  },
] as const;

export const storeById = (id: StoreId): Store =>
  stores.find((s) => s.id === id)!;
```

- [ ] **Step 3: Write `src/data/services.ts`**

```ts
export type Service = {
  id: "domicilio" | "catering" | "horeca";
  title: string;
  short: string;
  body: string;
  icon: "truck" | "party" | "store";
};

export const services: readonly Service[] = [
  {
    id: "domicilio",
    title: "Servicio a domicilio",
    short: "Recibe el pan en casa, de lunes a sábado.",
    body: "Pedidos por WhatsApp o teléfono antes de las 11:00, entrega esa misma tarde en Alcobendas, Pozuelo y municipios cercanos. Pedido mínimo 15 €.",
    icon: "truck",
  },
  {
    id: "catering",
    title: "Catering para eventos",
    short: "Bautizos, comuniones, comidas de empresa, bodas.",
    body: "Repostería salada y dulce a medida. Asesoría de menú, montaje y entrega. Pídenos presupuesto con al menos una semana de antelación.",
    icon: "party",
  },
  {
    id: "horeca",
    title: "Distribución a hostelería",
    short: "Bares, restaurantes, hoteles y cafeterías.",
    body: "Reparto diario de pan, bollería y panecillos. Catálogo profesional, precios mayoristas, facturación mensual. Cubrimos toda la zona norte y oeste de Madrid.",
    icon: "store",
  },
] as const;
```

- [ ] **Step 4: Write `src/data/categories.ts`**

```ts
export const categoryIds = [
  "panes",
  "bolleria",
  "tartas",
  "salado",
  "temporada",
] as const;

export type CategoryId = (typeof categoryIds)[number];

export type Category = {
  id: CategoryId;
  label: string;
  short: string;
};

export const categories: readonly Category[] = [
  { id: "panes", label: "Panes", short: "Masa madre, fermentación lenta" },
  {
    id: "bolleria",
    label: "Bollería",
    short: "Croissants, napolitanas, suizos",
  },
  { id: "tartas", label: "Tartas", short: "Para celebrar o capricho diario" },
  { id: "salado", label: "Salado", short: "Empanadas, quiches, hojaldres" },
  { id: "temporada", label: "Temporada", short: "Roscón, torrijas, panettone" },
] as const;

export const categoryById = (id: CategoryId): Category =>
  categories.find((c) => c.id === id)!;
```

- [ ] **Step 5: Write `src/data/tradicion.ts`**

```ts
export const tradicion = {
  intro:
    "Desde 1986 horneamos en Madrid el mismo pan que aprendimos a hacer en casa: masa madre viva, fermentaciones largas, harinas que sabemos de dónde vienen.",
  pillars: [
    {
      title: "Masa madre, no acelerada",
      body: "Fermentamos entre 18 y 24 horas según la pieza. El pan que sale del horno tiene corteza, alma y aguante para tres días sobre la mesa.",
    },
    {
      title: "Harina española trazable",
      body: "Trabajamos con molinos de Castilla y León y Aragón. Sabemos qué trigo es, dónde se cultivó y cómo se molió. Sin mezclas anónimas.",
    },
    {
      title: "Sin atajos químicos",
      body: "Cero mejorantes, cero conservantes, cero colorantes. La calidad no viene de un sobre; viene del tiempo y de las manos.",
    },
    {
      title: "El equipo que sigue",
      body: "Tres generaciones de panaderos. El obrador no para de noche para que la primera barra de la mañana esté caliente cuando abren las puertas.",
    },
  ],
  certifications: [
    "Pan 100 % artesano (Real Decreto 308/2019)",
    "Inscritos en el registro sanitario de Madrid desde 1986",
  ],
} as const;
```

- [ ] **Step 6: Type check**

Run: `pnpm check`
Expected: no errors.

- [ ] **Step 7: Commit**

```bash
git add src/data/
git commit -m "feat(data): site, stores, services, categories, tradicion"
```

---

### Task 5: Content collections schema

**Files:**

- Create: `src/content.config.ts`

- [ ] **Step 1: Write `src/content.config.ts`**

```ts
import { defineCollection, z } from "astro:content";
import { glob } from "astro/loaders";
import { categoryIds } from "~/data/categories";

const allergens = z.enum([
  "gluten",
  "huevo",
  "leche",
  "frutos-secos",
  "soja",
  "sesamo",
  "sulfitos",
]);

const variant = z.object({
  id: z.string(),
  label: z.string(),
  priceCents: z.number().int().positive(),
});

export const products = defineCollection({
  loader: glob({ pattern: "**/*.md", base: "./src/content/products" }),
  schema: ({ image }) =>
    z.object({
      name: z.string(),
      category: z.enum(categoryIds),
      priceCents: z.number().int().positive(),
      unit: z.string().optional(),
      variants: z.array(variant).optional(),
      image: image(),
      imageAlt: z.string(),
      shortDescription: z.string().max(180),
      allergens: z.array(allergens).default([]),
      featured: z.boolean().default(false),
      seasonal: z.boolean().default(false),
      order: z.number().int().default(100),
    }),
});

export const noticias = defineCollection({
  loader: glob({ pattern: "**/*.md", base: "./src/content/noticias" }),
  schema: ({ image }) =>
    z.object({
      title: z.string(),
      date: z.coerce.date(),
      excerpt: z.string().max(240),
      image: image(),
      imageAlt: z.string(),
      tags: z.array(z.string()).default([]),
      draft: z.boolean().default(false),
    }),
});

export const collections = { products, noticias };
```

- [ ] **Step 2: Verify schema typechecks (no content yet — collection will be empty)**

Run: `pnpm check`
Expected: no errors. (Astro generates `.astro/content.d.ts` from the schema.)

- [ ] **Step 3: Commit**

```bash
git add src/content.config.ts
git commit -m "feat(content): zod schemas for products and noticias"
```

---

### Task 6: Seed product content (~20 products)

**Files:**

- Create: `src/content/products/pan-de-pueblo.md`
- Create: `src/content/products/pan-integral.md`
- Create: `src/content/products/pan-candeal.md`
- Create: `src/content/products/hogaza-rustica.md`
- Create: `src/content/products/pan-de-cereales.md`
- Create: `src/content/products/baguette.md`
- Create: `src/content/products/croissant-mantequilla.md`
- Create: `src/content/products/napolitana-chocolate.md`
- Create: `src/content/products/ensaimada.md`
- Create: `src/content/products/palmera-chocolate.md`
- Create: `src/content/products/suizo.md`
- Create: `src/content/products/magdalena-casera.md`
- Create: `src/content/products/tarta-de-zanahoria.md`
- Create: `src/content/products/tarta-de-queso.md`
- Create: `src/content/products/sacher.md`
- Create: `src/content/products/tarta-de-manzana.md`
- Create: `src/content/products/tarta-tres-chocolates.md`
- Create: `src/content/products/empanada-gallega.md`
- Create: `src/content/products/quiche-lorraine.md`
- Create: `src/content/products/hojaldre-jamon-y-queso.md`
- Create: `src/content/products/roscon-de-reyes.md`
- Create: `src/content/products/torrijas.md`
- Create: `src/content/products/panettone.md`

- [ ] **Step 1: Write the canonical example `src/content/products/pan-de-pueblo.md`**

```markdown
---
name: Pan de pueblo
category: panes
priceCents: 325
unit: "500 g"
variants:
  - { id: "500g", label: "500 g", priceCents: 325 }
  - { id: "1kg", label: "1 kg", priceCents: 600 }
image: ../../../public/images/productos/pan-de-pueblo.jpg
imageAlt: "Pan de pueblo recién horneado, partido por la mitad"
shortDescription: Masa madre, fermentación lenta de 18 horas. Corteza crujiente, miga alveolada.
allergens: [gluten]
featured: true
seasonal: false
order: 10
---

Pan elaborado con harina de trigo española de molienda lenta y masa madre viva. Fermentación
controlada durante 18 horas a baja temperatura, horneado en suelo de piedra.

La pieza de 1 kg aguanta perfectamente 3 días en panera de tela; congelado, hasta 2 meses.
Se recomienda tostarlo el último día.
```

- [ ] **Step 2: Write the rest of `panes` (5 archivos)**

Use the same shape. Image path matches `/images/productos/<slug>.jpg` and the relative loader path `../../../public/images/productos/<slug>.jpg`. Filled values:

```markdown
---
name: Pan integral 100 %
category: panes
priceCents: 380
unit: "500 g"
image: ../../../public/images/productos/pan-integral.jpg
imageAlt: "Pan integral cortado, miga densa y oscura"
shortDescription: Harina integral de trigo y centeno, semillas de girasol y lino. Saciante, nutritivo.
allergens: [gluten]
featured: false
order: 20
---

Pan denso y aromático con harina integral 100 %. Fermentación con masa madre, sin levadura industrial.
```

```markdown
---
name: Pan candeal
category: panes
priceCents: 350
unit: "500 g"
image: ../../../public/images/productos/pan-candeal.jpg
imageAlt: "Pan candeal con greñado en forma de espiga"
shortDescription: Miga prieta y blanca, corteza fina y dorada. El pan de toda la vida de Castilla.
allergens: [gluten]
order: 30
---

Elaborado con harina refinada de fuerza, agua, sal y masa madre. Sin grasa añadida.
```

```markdown
---
name: Hogaza rústica
category: panes
priceCents: 525
unit: "750 g"
image: ../../../public/images/productos/hogaza-rustica.jpg
imageAlt: "Hogaza redonda con corteza muy oscura, sobre tabla de madera"
shortDescription: Hogaza grande con harina T80 y un toque de centeno. Corteza gruesa, miga jugosa.
allergens: [gluten]
featured: true
order: 40
---

Fermentación de 24 horas. Aguanta 4 días, ideal para tostadas y bocadillos generosos.
```

```markdown
---
name: Pan de cereales
category: panes
priceCents: 420
unit: "500 g"
image: ../../../public/images/productos/pan-de-cereales.jpg
imageAlt: "Pan rebozado con semillas variadas"
shortDescription: Cinco semillas: lino, girasol, sésamo, calabaza y amapola. Pan completo y sabroso.
allergens: [gluten, sesamo]
order: 50
---

Harina mixta de trigo y centeno, semillas tostadas dentro y fuera de la pieza.
```

```markdown
---
name: Baguette
category: panes
priceCents: 195
unit: "250 g"
image: ../../../public/images/productos/baguette.jpg
imageAlt: "Baguette larga con cortes diagonales"
shortDescription: Baguette tradicional, corteza fina y crujiente, miga alveolada y aireada.
allergens: [gluten]
order: 60
---

Fermentación de 12 horas. Perfecta para bocadillos al instante y para mojar.
```

- [ ] **Step 3: Write `bolleria` (6 archivos)**

```markdown
---
name: Croissant de mantequilla
category: bolleria
priceCents: 165
image: ../../../public/images/productos/croissant-mantequilla.jpg
imageAlt: "Croissant dorado con hojaldre marcado"
shortDescription: Hojaldre con mantequilla francesa, 32 capas. Tierno por dentro, crujiente por fuera.
allergens: [gluten, leche, huevo]
featured: true
order: 110
---

Mantequilla AOP de Charentes. Sin margarina, sin aceites vegetales.
```

```markdown
---
name: Napolitana de chocolate
category: bolleria
priceCents: 185
image: ../../../public/images/productos/napolitana-chocolate.jpg
imageAlt: "Napolitana rellena de chocolate, recién horneada"
shortDescription: Hojaldre relleno de chocolate 70 %, terminado al horno justo antes de servir.
allergens: [gluten, leche, huevo, soja]
order: 120
---
```

```markdown
---
name: Ensaimada
category: bolleria
priceCents: 220
image: ../../../public/images/productos/ensaimada.jpg
imageAlt: "Ensaimada espolvoreada con azúcar glas"
shortDescription: Receta mallorquina tradicional, con manteca ibérica y largo reposo en frío.
allergens: [gluten, huevo]
order: 130
---
```

```markdown
---
name: Palmera de chocolate
category: bolleria
priceCents: 195
image: ../../../public/images/productos/palmera-chocolate.jpg
imageAlt: "Palmera bañada en chocolate negro"
shortDescription: Hojaldre caramelizado, bañada en chocolate negro 60 %. Brutal con el café.
allergens: [gluten, leche, soja]
order: 140
---
```

```markdown
---
name: Suizo
category: bolleria
priceCents: 145
image: ../../../public/images/productos/suizo.jpg
imageAlt: "Suizo con azúcar grueso por encima"
shortDescription: Bollo de leche tierno, terminado con azúcar grueso. Para mojar en el café.
allergens: [gluten, leche, huevo]
order: 150
---
```

```markdown
---
name: Magdalena casera
category: bolleria
priceCents: 95
image: ../../../public/images/productos/magdalena-casera.jpg
imageAlt: "Magdalena con copete dorado, en cápsula de papel"
shortDescription: Aceite de oliva virgen, ralladura de limón. Esponjosa, con copete generoso.
allergens: [gluten, huevo]
featured: true
order: 160
---
```

- [ ] **Step 4: Write `tartas` (5 archivos)**

```markdown
---
name: Tarta de zanahoria
category: tartas
priceCents: 1800
unit: "para 6 personas"
variants:
  - { id: "6p", label: "6 personas", priceCents: 1800 }
  - { id: "10p", label: "10 personas", priceCents: 2800 }
image: ../../../public/images/productos/tarta-de-zanahoria.jpg
imageAlt: "Porción de tarta de zanahoria con frosting de queso"
shortDescription: Bizcocho especiado, zanahoria recién rallada, frosting de queso. Reservar 24 h antes.
allergens: [gluten, huevo, leche, frutos-secos]
featured: true
order: 210
---
```

```markdown
---
name: Tarta de queso
category: tartas
priceCents: 2200
unit: "para 8 personas"
image: ../../../public/images/productos/tarta-de-queso.jpg
imageAlt: "Tarta de queso al horno, dorada y cremosa"
shortDescription: Estilo La Viña: queso crema, nata, huevo y nada más. Centro cremoso, costra dorada.
allergens: [huevo, leche]
order: 220
---
```

```markdown
---
name: Sacher
category: tartas
priceCents: 2400
unit: "para 8 personas"
image: ../../../public/images/productos/sacher.jpg
imageAlt: "Tarta Sacher cubierta de glaseado de chocolate brillante"
shortDescription: Bizcocho de chocolate, capa de mermelada de albaricoque, glaseado espejo.
allergens: [gluten, huevo, leche, soja]
order: 230
---
```

```markdown
---
name: Tarta de manzana
category: tartas
priceCents: 1900
unit: "para 6 personas"
image: ../../../public/images/productos/tarta-de-manzana.jpg
imageAlt: "Tarta de manzana laminada, brillante, en molde rectangular"
shortDescription: Hojaldre, crema pastelera ligera, manzana reineta laminada y caramelizada.
allergens: [gluten, huevo, leche]
order: 240
---
```

```markdown
---
name: Tarta tres chocolates
category: tartas
priceCents: 2100
unit: "para 8 personas"
image: ../../../public/images/productos/tarta-tres-chocolates.jpg
imageAlt: "Tarta con tres capas: chocolate blanco, con leche y negro"
shortDescription: Tres mousses sobre base de galleta. Refrigerada, lista al momento.
allergens: [gluten, leche, soja]
order: 250
---
```

- [ ] **Step 5: Write `salado` (3 archivos)**

```markdown
---
name: Empanada gallega de atún
category: salado
priceCents: 1450
unit: "para 4 personas"
image: ../../../public/images/productos/empanada-gallega.jpg
imageAlt: "Empanada gallega con masa dorada y sofrito visible"
shortDescription: Masa fina con aceite de oliva, sofrito de cebolla y pimiento, atún del Cantábrico.
allergens: [gluten, huevo]
order: 310
---
```

```markdown
---
name: Quiche lorraine
category: salado
priceCents: 1650
unit: "para 4 personas"
image: ../../../public/images/productos/quiche-lorraine.jpg
imageAlt: "Quiche lorraine con bacon y cebolla, dorada por encima"
shortDescription: Masa brisa casera, panceta ahumada, nata y huevo. Estilo francés tradicional.
allergens: [gluten, huevo, leche]
order: 320
---
```

```markdown
---
name: Hojaldre de jamón y queso
category: salado
priceCents: 250
image: ../../../public/images/productos/hojaldre-jamon-y-queso.jpg
imageAlt: "Hojaldre rectangular dorado relleno de jamón y queso"
shortDescription: Hojaldre con mantequilla, jamón cocido extra y queso gouda fundido.
allergens: [gluten, leche, huevo]
order: 330
---
```

- [ ] **Step 6: Write `temporada` (3 archivos)**

```markdown
---
name: Roscón de Reyes
category: temporada
priceCents: 2200
unit: "para 8 personas"
variants:
  - { id: "sin-relleno", label: "Sin relleno", priceCents: 2200 }
  - { id: "nata", label: "Relleno de nata", priceCents: 2600 }
  - { id: "crema", label: "Relleno de crema", priceCents: 2600 }
  - { id: "trufa", label: "Relleno de trufa", priceCents: 2800 }
image: ../../../public/images/productos/roscon-de-reyes.jpg
imageAlt: "Roscón de Reyes con fruta escarchada y azúcar"
shortDescription: Brioche tradicional con agua de azahar y naranja confitada. Reservas a partir de diciembre.
allergens: [gluten, huevo, leche]
featured: true
seasonal: true
order: 410
---
```

```markdown
---
name: Torrijas de Semana Santa
category: temporada
priceCents: 320
unit: "unidad"
image: ../../../public/images/productos/torrijas.jpg
imageAlt: "Torrija dorada en almíbar de miel"
shortDescription: Pan brioche del día anterior, leche infusionada con canela, fritura al momento.
allergens: [gluten, huevo, leche]
seasonal: true
order: 420
---
```

```markdown
---
name: Panettone artesano
category: temporada
priceCents: 2800
unit: "750 g"
image: ../../../public/images/productos/panettone.jpg
imageAlt: "Panettone alto cortado por la mitad, miga amarilla con fruta"
shortDescription: Masa madre activa, 48 h de fermentación. Frutas confitadas o gotas de chocolate.
allergens: [gluten, huevo, leche]
featured: true
seasonal: true
order: 430
---
```

- [ ] **Step 7: Type check that all products validate**

Run: `pnpm check`
Expected: no errors. Images don't exist yet — Astro will complain at build time, but `check` should pass since image schemas are validated only at build.

If `astro:content` errors because images are missing: skip ahead and run Task 7 first (it doesn't need images), then run Task 25 (image download) before any `pnpm build`. For `pnpm dev` and `pnpm check`, missing images are tolerated.

- [ ] **Step 8: Commit**

```bash
git add src/content/products/
git commit -m "feat(content): seed ~22 products across 5 categories"
```

---

### Task 7: Seed noticias content

**Files:**

- Create: `src/content/noticias/roscon-reyes-2026.md`
- Create: `src/content/noticias/san-valentin-2026.md`
- Create: `src/content/noticias/dia-del-padre-2026.md`
- Create: `src/content/noticias/san-lorenzo-2025.md`
- Create: `src/content/noticias/navidad-2025.md`

- [ ] **Step 1: Write each noticia**

`roscon-reyes-2026.md`:

```markdown
---
title: Roscón de Reyes 2026 — reservas abiertas
date: 2025-12-15
excerpt: Como cada año, pero mejor. Nata, trufa, crema o sin nada — tú eliges. Reserva tu unidad antes del 3 de enero.
image: ../../../public/images/noticias/roscon-2026.jpg
imageAlt: "Roscón de Reyes con nata, decorado con fruta escarchada"
tags: [temporada, navidad, reservas]
---

El roscón es el postre de la casa: masa madre, agua de azahar de Carcaixent, naranja confitada
artesana, fermentación de 24 horas. Cuatro versiones: sin relleno, nata, crema pastelera o trufa.

**Reservas:** WhatsApp o en tienda. Hasta el 3 de enero. Recogida 5 y 6 de enero.
```

`san-valentin-2026.md`:

```markdown
---
title: San Valentín — tarta corazón y bombones
date: 2026-02-01
excerpt: Tarta de chocolate con forma de corazón, bombones a granel y nuestra caja de regalo.
image: ../../../public/images/noticias/san-valentin.jpg
imageAlt: "Tarta de chocolate en forma de corazón con frambuesas"
tags: [temporada, san-valentin]
---

Tarta de chocolate y frambuesa con forma de corazón (6 u 8 personas), bombones artesanos a granel
y una caja de regalo para sorprender. Pide la tarta con 48 horas de antelación.
```

`dia-del-padre-2026.md`:

```markdown
---
title: Día del Padre — corbata de hojaldre
date: 2026-03-10
excerpt: Nuestro hojaldre relleno de crema y chocolate, en forma de corbata. Tradición desde 1986.
image: ../../../public/images/noticias/dia-del-padre.jpg
imageAlt: "Corbata de hojaldre rellena, con baño de chocolate"
tags: [temporada, dia-del-padre]
---

Una corbata de hojaldre rellena de crema pastelera y chocolate, bañada en glaseado.
Disponible del 15 al 19 de marzo.
```

`san-lorenzo-2025.md`:

```markdown
---
title: 10 de agosto — fiesta de San Lorenzo
date: 2025-08-05
excerpt: Nuestro patrón. Tortas de aceite, magdalenas y limonada gratis para clientes durante todo el día.
image: ../../../public/images/noticias/san-lorenzo.jpg
imageAlt: "Tortas de aceite tradicionales sobre tabla de madera"
tags: [tradicion, fiestas]
---

San Lorenzo es nuestro patrón y el motivo del nombre del horno. El 10 de agosto invitamos
a torta de aceite y limonada a quienes pasen por tienda.
```

`navidad-2025.md`:

```markdown
---
title: Navidad 2025 — pedidos para Nochebuena y Año Nuevo
date: 2025-12-01
excerpt: Polvorones, mantecados, panettone artesano y nuestro catering de canapés para celebrar en casa.
image: ../../../public/images/noticias/navidad.jpg
imageAlt: "Bandeja de surtido navideño con polvorones y mantecados"
tags: [temporada, navidad]
---

Pedidos hasta el 22 de diciembre. Recogida 24 y 31 de diciembre por la mañana.
Servicio de catering de canapés y empanadillas para grupos a partir de 8 personas.
```

- [ ] **Step 2: Type check**

Run: `pnpm check`
Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add src/content/noticias/
git commit -m "feat(content): seed 5 noticias (Roscón, San Valentín, Padre, San Lorenzo, Navidad)"
```

---

## Phase 4 — Cart logic + utilities

### Task 8: format.ts utilities

**Files:**

- Create: `src/lib/format.ts`

- [ ] **Step 1: Write `src/lib/format.ts`**

```ts
export function formatPriceCents(cents: number): string {
  return new Intl.NumberFormat("es-ES", {
    style: "currency",
    currency: "EUR",
    minimumFractionDigits: 2,
  }).format(cents / 100);
}

export function formatDateISO(iso: string): string {
  const d = new Date(iso + "T00:00:00");
  return new Intl.DateTimeFormat("es-ES", {
    weekday: "long",
    day: "2-digit",
    month: "2-digit",
    year: "numeric",
  }).format(d);
}

export function formatDate(d: Date): string {
  return new Intl.DateTimeFormat("es-ES", {
    day: "2-digit",
    month: "short",
    year: "numeric",
  }).format(d);
}

export function slugifyVariant(slug: string, variantId?: string): string {
  return variantId ? `${slug}__${variantId}` : slug;
}
```

- [ ] **Step 2: Manual verification (in Node REPL)**

Run:

```bash
node --input-type=module -e "
import('./src/lib/format.ts').catch(()=>{});
import('./src/lib/format.js').catch(()=>{});
"
```

That won't work directly (TS file). Use a quick compile-and-run instead:

```bash
pnpm exec tsx -e "
import { formatPriceCents, formatDateISO } from './src/lib/format';
console.log(formatPriceCents(325));        // '3,25 €'
console.log(formatPriceCents(2200));       // '22,00 €'
console.log(formatDateISO('2026-05-22'));  // 'viernes, 22/05/2026'
"
```

If `tsx` not installed: `pnpm dlx tsx@latest -e "..."`.

Expected output:

```
3,25 €
22,00 €
viernes, 22/05/2026
```

- [ ] **Step 3: Commit**

```bash
git add src/lib/format.ts
git commit -m "feat(lib): price/date formatters (es-ES)"
```

---

### Task 9: cart.ts pure functions

**Files:**

- Create: `src/lib/cart.ts`

- [ ] **Step 1: Write `src/lib/cart.ts`**

```ts
export type CartItem = {
  slug: string;
  name: string;
  variantId?: string;
  variantLabel?: string;
  qty: number;
  unitPriceCents: number;
};

export type Cart = {
  items: CartItem[];
  updatedAt: string;
};

const STORAGE_KEY = "hsl-cart-v1";
const CART_EVENT = "hsl-cart-update";

const isBrowser = typeof window !== "undefined";

function emptyCart(): Cart {
  return { items: [], updatedAt: new Date().toISOString() };
}

export function loadCart(): Cart {
  if (!isBrowser) return emptyCart();
  try {
    const raw = window.localStorage.getItem(STORAGE_KEY);
    if (!raw) return emptyCart();
    const parsed = JSON.parse(raw) as Cart;
    if (!Array.isArray(parsed.items)) return emptyCart();
    parsed.items = parsed.items.filter(
      (i) =>
        typeof i.slug === "string" &&
        typeof i.qty === "number" &&
        i.qty > 0 &&
        typeof i.unitPriceCents === "number" &&
        i.unitPriceCents > 0,
    );
    return parsed;
  } catch {
    return emptyCart();
  }
}

function saveCart(cart: Cart): void {
  if (!isBrowser) return;
  cart.updatedAt = new Date().toISOString();
  window.localStorage.setItem(STORAGE_KEY, JSON.stringify(cart));
  window.dispatchEvent(new CustomEvent(CART_EVENT, { detail: cart }));
}

function sameItem(
  a: CartItem,
  b: Pick<CartItem, "slug" | "variantId">,
): boolean {
  return a.slug === b.slug && (a.variantId ?? null) === (b.variantId ?? null);
}

export function addItem(item: Omit<CartItem, "qty"> & { qty?: number }): Cart {
  const cart = loadCart();
  const qty = item.qty ?? 1;
  const existing = cart.items.find((i) => sameItem(i, item));
  if (existing) {
    existing.qty += qty;
  } else {
    cart.items.push({ ...item, qty });
  }
  saveCart(cart);
  return cart;
}

export function updateQty(
  slug: string,
  variantId: string | undefined,
  qty: number,
): Cart {
  const cart = loadCart();
  const item = cart.items.find((i) => sameItem(i, { slug, variantId }));
  if (!item) return cart;
  if (qty <= 0) {
    cart.items = cart.items.filter((i) => !sameItem(i, { slug, variantId }));
  } else {
    item.qty = qty;
  }
  saveCart(cart);
  return cart;
}

export function removeItem(slug: string, variantId?: string): Cart {
  return updateQty(slug, variantId, 0);
}

export function clearCart(): Cart {
  const cart = emptyCart();
  saveCart(cart);
  return cart;
}

export function totalCents(cart: Cart): number {
  return cart.items.reduce((sum, i) => sum + i.unitPriceCents * i.qty, 0);
}

export function totalQty(cart: Cart): number {
  return cart.items.reduce((sum, i) => sum + i.qty, 0);
}

export const CART_UPDATE_EVENT = CART_EVENT;
```

- [ ] **Step 2: Manual verification**

```bash
pnpm dlx tsx@latest -e "
import { addItem, updateQty, removeItem, clearCart, loadCart, totalCents, totalQty } from './src/lib/cart';

// fake browser storage
const store = new Map();
globalThis.window = {
  localStorage: {
    getItem: (k) => store.get(k) ?? null,
    setItem: (k, v) => store.set(k, v),
    removeItem: (k) => store.delete(k),
  },
  dispatchEvent: () => true,
};

clearCart();
addItem({ slug: 'pan-de-pueblo', name: 'Pan de pueblo', variantId: '1kg', variantLabel: '1 kg', unitPriceCents: 600, qty: 2 });
addItem({ slug: 'pan-de-pueblo', name: 'Pan de pueblo', variantId: '1kg', variantLabel: '1 kg', unitPriceCents: 600, qty: 1 });
addItem({ slug: 'tarta-de-zanahoria', name: 'Tarta de zanahoria', unitPriceCents: 1800, qty: 1 });

const c = loadCart();
console.log('items:', c.items.length);             // 2
console.log('qty pan:', c.items[0].qty);           // 3
console.log('total cents:', totalCents(c));        // 600*3 + 1800 = 3600
console.log('total qty:', totalQty(c));            // 4

updateQty('pan-de-pueblo', '1kg', 1);
console.log('after update qty:', totalQty(loadCart())); // 2

removeItem('tarta-de-zanahoria');
console.log('after remove items:', loadCart().items.length); // 1
"
```

Expected output:

```
items: 2
qty pan: 3
total cents: 3600
total qty: 4
after update qty: 2
after remove items: 1
```

- [ ] **Step 3: Commit**

```bash
git add src/lib/cart.ts
git commit -m "feat(cart): pure cart functions with localStorage + cart-update event"
```

---

### Task 10: whatsapp.ts message builder

**Files:**

- Create: `src/lib/whatsapp.ts`

- [ ] **Step 1: Write `src/lib/whatsapp.ts`**

```ts
import { storeById, type StoreId } from "~/data/stores";
import type { Cart } from "~/lib/cart";
import { formatPriceCents, formatDateISO } from "~/lib/format";

export type PickupInfo = {
  storeId: StoreId;
  dateISO: string;
  slot: "morning" | "afternoon";
  name?: string;
  notes?: string;
};

const SLOT_LABEL: Record<PickupInfo["slot"], string> = {
  morning: "mañana",
  afternoon: "tarde",
};

export function buildWhatsAppMessage(cart: Cart, pickup: PickupInfo): string {
  const store = storeById(pickup.storeId);

  const lines: string[] = [];
  lines.push("Hola Horno San Lorenzo 👋", "");
  lines.push("Quería hacer un pedido:", "");

  for (const item of cart.items) {
    const label = item.variantLabel ? ` (${item.variantLabel})` : "";
    const line = `• ${item.qty}× ${item.name}${label} — ${formatPriceCents(
      item.unitPriceCents * item.qty,
    )}`;
    lines.push(line);
  }

  const total = cart.items.reduce((s, i) => s + i.unitPriceCents * i.qty, 0);
  lines.push("", `Total estimado: ${formatPriceCents(total)}`, "");
  lines.push(`📍 Recogida: ${store.shortName} (${store.address})`);
  lines.push(`📅 Día: ${formatDateISO(pickup.dateISO)}`);
  lines.push(`🕘 Franja: ${SLOT_LABEL[pickup.slot]}`);

  if (pickup.name) lines.push(`👤 Nombre: ${pickup.name}`);
  if (pickup.notes) lines.push(`📝 Notas: ${pickup.notes}`);

  lines.push("", "¿Me confirmáis disponibilidad y hora exacta?");

  return lines.join("\n");
}

export function buildWhatsAppUrl(phone: string, message: string): string {
  const cleaned = phone.replace(/[^\d]/g, "");
  return `https://wa.me/${cleaned}?text=${encodeURIComponent(message)}`;
}
```

- [ ] **Step 2: Manual verification**

```bash
pnpm dlx tsx@latest -e "
import { buildWhatsAppMessage, buildWhatsAppUrl } from './src/lib/whatsapp';

const cart = {
  updatedAt: '2026-05-19T10:00:00Z',
  items: [
    { slug: 'pan-de-pueblo',     name: 'Pan de pueblo',     variantId: '1kg', variantLabel: '1 kg', qty: 2, unitPriceCents: 600 },
    { slug: 'tarta-de-zanahoria', name: 'Tarta de zanahoria',                            qty: 1, unitPriceCents: 1800 },
    { slug: 'croissant-mantequilla', name: 'Croissant de mantequilla',                   qty: 6, unitPriceCents: 165 },
  ],
};

const pickup = {
  storeId: 'pozuelo',
  dateISO: '2026-05-22',
  slot: 'morning',
  name: 'Óscar Sánchez',
};

const msg = buildWhatsAppMessage(cart, pickup);
console.log(msg);
console.log('---');
console.log(buildWhatsAppUrl('34666123456', msg).slice(0, 80) + '...');
"
```

Expected: human-readable message matching the spec example, then a `https://wa.me/34666123456?text=...` URL.

- [ ] **Step 3: Commit**

```bash
git add src/lib/whatsapp.ts
git commit -m "feat(lib): WhatsApp message + URL builders"
```

---

### Task 11: useCart hook

**Files:**

- Create: `src/hooks/useCart.ts`

- [ ] **Step 1: Write `src/hooks/useCart.ts`**

```ts
import { useEffect, useState, useCallback } from "react";
import {
  loadCart,
  totalCents,
  totalQty,
  addItem as _addItem,
  updateQty as _updateQty,
  removeItem as _removeItem,
  clearCart as _clearCart,
  CART_UPDATE_EVENT,
  type Cart,
  type CartItem,
} from "~/lib/cart";

export function useCart() {
  const [cart, setCart] = useState<Cart>(() => loadCart());

  useEffect(() => {
    const handler = () => setCart(loadCart());
    window.addEventListener(CART_UPDATE_EVENT, handler);
    window.addEventListener("storage", handler);
    return () => {
      window.removeEventListener(CART_UPDATE_EVENT, handler);
      window.removeEventListener("storage", handler);
    };
  }, []);

  const addItem = useCallback(
    (item: Omit<CartItem, "qty"> & { qty?: number }) => {
      setCart(_addItem(item));
    },
    [],
  );

  const updateQty = useCallback(
    (slug: string, variantId: string | undefined, qty: number) => {
      setCart(_updateQty(slug, variantId, qty));
    },
    [],
  );

  const removeItem = useCallback((slug: string, variantId?: string) => {
    setCart(_removeItem(slug, variantId));
  }, []);

  const clearCart = useCallback(() => {
    setCart(_clearCart());
  }, []);

  return {
    cart,
    totalCents: totalCents(cart),
    totalQty: totalQty(cart),
    addItem,
    updateQty,
    removeItem,
    clearCart,
  };
}
```

- [ ] **Step 2: Type check**

Run: `pnpm check`
Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add src/hooks/useCart.ts
git commit -m "feat(hooks): useCart with cross-island sync via custom + storage events"
```

---

## Phase 5 — Layouts and chrome

### Task 12: SEO component

**Files:**

- Create: `src/components/SEO.astro`

- [ ] **Step 1: Write `src/components/SEO.astro`**

```astro
---
import { site } from "~/data/site";

interface Props {
  title: string;
  description: string;
  image?: string;
  canonical?: string;
  type?: "website" | "article" | "product";
  jsonLd?: Record<string, unknown> | Array<Record<string, unknown>>;
}

const {
  title,
  description,
  image = site.ogImage,
  canonical = Astro.url.pathname,
  type = "website",
  jsonLd,
} = Astro.props;

const fullTitle = title === site.name ? title : `${title} · ${site.name}`;
const url = new URL(canonical, Astro.site ?? site.url).toString();
const imageUrl = new URL(image, Astro.site ?? site.url).toString();

const jsonLdItems = jsonLd
  ? Array.isArray(jsonLd) ? jsonLd : [jsonLd]
  : [];
---
<title>{fullTitle}</title>
<meta name="description" content={description} />
<link rel="canonical" href={url} />

<meta property="og:type" content={type} />
<meta property="og:title" content={fullTitle} />
<meta property="og:description" content={description} />
<meta property="og:url" content={url} />
<meta property="og:image" content={imageUrl} />
<meta property="og:site_name" content={site.name} />
<meta property="og:locale" content="es_ES" />

<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content={fullTitle} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={imageUrl} />

<meta name="geo.region" content="ES-M" />
<meta name="geo.placename" content="Alcobendas; Pozuelo de Alarcón" />

{jsonLdItems.map((item) => (
  <script type="application/ld+json" set:html={JSON.stringify(item)} />
))}
```

- [ ] **Step 2: Type check**

Run: `pnpm check`
Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add src/components/SEO.astro
git commit -m "feat(seo): SEO component with og, twitter, jsonld"
```

---

### Task 13: ToastHost island

**Files:**

- Create: `src/islands/ToastHost.tsx`

- [ ] **Step 1: Write `src/islands/ToastHost.tsx`**

```tsx
import { useEffect, useState } from "react";

const TOAST_EVENT = "hsl-toast";

type Toast = { id: number; message: string };

export function emitToast(message: string): void {
  if (typeof window === "undefined") return;
  window.dispatchEvent(
    new CustomEvent<{ message: string }>(TOAST_EVENT, { detail: { message } }),
  );
}

export default function ToastHost() {
  const [toasts, setToasts] = useState<Toast[]>([]);

  useEffect(() => {
    const handler = (e: Event) => {
      const ce = e as CustomEvent<{ message: string }>;
      const id = Date.now() + Math.random();
      setToasts((prev) => [...prev, { id, message: ce.detail.message }]);
      setTimeout(() => {
        setToasts((prev) => prev.filter((t) => t.id !== id));
      }, 2800);
    };
    window.addEventListener(TOAST_EVENT, handler);
    return () => window.removeEventListener(TOAST_EVENT, handler);
  }, []);

  return (
    <div
      aria-live="polite"
      aria-atomic="true"
      style={{
        position: "fixed",
        bottom: "1rem",
        left: 0,
        right: 0,
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        gap: "0.5rem",
        pointerEvents: "none",
        zIndex: 60,
      }}
    >
      {toasts.map((t) => (
        <div
          key={t.id}
          style={{
            background: "var(--color-ink)",
            color: "var(--color-cream)",
            padding: "0.75rem 1.25rem",
            borderRadius: 10,
            boxShadow: "var(--shadow-toast)",
            display: "inline-flex",
            alignItems: "center",
            gap: "0.625rem",
            fontSize: 14,
            animation: "hsl-toast-in .25s ease-out both",
          }}
        >
          <span
            style={{
              background: "var(--color-wa)",
              color: "white",
              width: 22,
              height: 22,
              borderRadius: "50%",
              display: "inline-flex",
              alignItems: "center",
              justifyContent: "center",
              fontSize: 13,
              fontWeight: 900,
            }}
          >
            ✓
          </span>
          {t.message}
        </div>
      ))}
      <style>{`
        @keyframes hsl-toast-in {
          from { opacity: 0; transform: translateY(8px); }
          to   { opacity: 1; transform: translateY(0); }
        }
      `}</style>
    </div>
  );
}
```

- [ ] **Step 2: Type check**

Run: `pnpm check`
Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add src/islands/ToastHost.tsx
git commit -m "feat(islands): toast host that listens to hsl-toast events"
```

---

### Task 14: CartBadge island

**Files:**

- Create: `src/islands/CartBadge.tsx`

- [ ] **Step 1: Write `src/islands/CartBadge.tsx`**

```tsx
import { useCart } from "~/hooks/useCart";

export default function CartBadge() {
  const { totalQty } = useCart();

  return (
    <a
      href="/carrito"
      aria-label={`Ver pedido (${totalQty} unidades)`}
      style={{
        display: "inline-flex",
        alignItems: "center",
        gap: "0.5rem",
        background: "var(--color-ink)",
        color: "var(--color-cream)",
        padding: "0.5rem 0.875rem",
        borderRadius: 9999,
        fontSize: 13,
        fontWeight: 600,
        textDecoration: "none",
        transition: "transform .15s",
      }}
    >
      <span>Mi pedido</span>
      <span
        style={{
          background:
            totalQty > 0 ? "var(--color-hot)" : "var(--color-ink-muted)",
          color: "white",
          borderRadius: 9999,
          minWidth: 22,
          height: 22,
          padding: "0 7px",
          fontSize: 11,
          display: "inline-flex",
          alignItems: "center",
          justifyContent: "center",
        }}
      >
        {totalQty}
      </span>
    </a>
  );
}
```

- [ ] **Step 2: Type check**

Run: `pnpm check`
Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add src/islands/CartBadge.tsx
git commit -m "feat(islands): CartBadge with live qty from useCart"
```

---

### Task 15: Header, Footer, SkipLink

**Files:**

- Create: `src/components/SkipLink.astro`
- Create: `src/components/Header.astro`
- Create: `src/components/Footer.astro`

- [ ] **Step 1: Write `src/components/SkipLink.astro`**

```astro
---
---
<a href="#main" class="skip-link">Saltar al contenido</a>
```

- [ ] **Step 2: Write `src/components/Header.astro`**

```astro
---
import CartBadge from "~/islands/CartBadge.tsx";
import { site } from "~/data/site";

const links = [
  { href: "/catalogo",            label: "Catálogo" },
  { href: "/servicios",           label: "Servicios" },
  { href: "/noticias",            label: "Noticias" },
  { href: "/tradicion-y-calidad", label: "Tradición" },
  { href: "/tiendas",             label: "Tiendas" },
  { href: "/contacto",            label: "Contacto" },
];

const here = Astro.url.pathname;
---
<header
  class="border-b border-[color:var(--color-line)] bg-[color:var(--color-cream)] sticky top-0 z-50 backdrop-blur"
  transition:persist
>
  <div class="mx-auto max-w-6xl px-4 sm:px-6 py-3 flex items-center justify-between gap-4">
    <a href="/" class="flex flex-col leading-none" aria-label={`${site.name} — inicio`}>
      <span class="font-[family-name:var(--font-display)] italic text-[color:var(--color-ink)] text-lg sm:text-xl font-semibold">
        Horno San Lorenzo
      </span>
      <span class="text-[10px] uppercase tracking-[0.2em] text-[color:var(--color-ink-muted)] mt-0.5 font-semibold">
        desde 1986
      </span>
    </a>

    <nav aria-label="Principal" class="hidden md:flex items-center gap-5 text-sm">
      {links.map((l) => (
        <a
          href={l.href}
          aria-current={here.startsWith(l.href) ? "page" : undefined}
          class:list={[
            "text-[color:var(--color-ink-muted)] hover:text-[color:var(--color-ink)]",
            here.startsWith(l.href) && "text-[color:var(--color-ink)] font-semibold",
          ]}
        >
          {l.label}
        </a>
      ))}
    </nav>

    <div class="flex items-center gap-3">
      <CartBadge client:only="react" />
      <button
        class="md:hidden inline-flex items-center justify-center w-9 h-9 rounded-full border border-[color:var(--color-line)] text-[color:var(--color-ink)]"
        aria-label="Abrir menú"
        onclick="document.getElementById('mobile-nav').classList.toggle('hidden')"
        type="button"
      >
        ☰
      </button>
    </div>
  </div>

  <nav id="mobile-nav" aria-label="Móvil" class="md:hidden hidden border-t border-[color:var(--color-line)] bg-[color:var(--color-cream)]">
    <div class="mx-auto max-w-6xl px-4 py-3 flex flex-col gap-3 text-sm">
      {links.map((l) => (
        <a href={l.href} class="py-1 text-[color:var(--color-ink)]">{l.label}</a>
      ))}
    </div>
  </nav>
</header>
```

- [ ] **Step 3: Write `src/components/Footer.astro`**

```astro
---
import { site } from "~/data/site";
import { stores } from "~/data/stores";
const year = new Date().getFullYear();
---
<footer class="bg-[color:var(--color-cream)] border-t border-[color:var(--color-line)] mt-24" transition:persist>
  <div class="mx-auto max-w-6xl px-4 sm:px-6 py-12 grid gap-8 md:grid-cols-4 text-sm">
    <div>
      <p class="font-[family-name:var(--font-display)] italic text-lg text-[color:var(--color-ink)]">{site.name}</p>
      <p class="mt-2 text-[color:var(--color-ink-muted)] max-w-xs">{site.tagline}</p>
      <p class="mt-4 text-xs text-[color:var(--color-ink-muted)]">{site.noteForDemo}</p>
    </div>
    <div>
      <p class="text-[10px] uppercase tracking-[0.2em] text-[color:var(--color-ink-muted)] font-semibold">Tiendas</p>
      <ul class="mt-3 space-y-3">
        {stores.map((s) => (
          <li>
            <p class="font-semibold text-[color:var(--color-ink)]">{s.shortName}</p>
            <p class="text-[color:var(--color-ink-muted)]">{s.address}</p>
            <p class="text-[color:var(--color-ink-muted)]">{s.phoneDisplay}</p>
          </li>
        ))}
      </ul>
    </div>
    <div>
      <p class="text-[10px] uppercase tracking-[0.2em] text-[color:var(--color-ink-muted)] font-semibold">Navegación</p>
      <ul class="mt-3 space-y-2">
        <li><a href="/catalogo">Catálogo</a></li>
        <li><a href="/servicios">Servicios</a></li>
        <li><a href="/tradicion-y-calidad">Tradición y calidad</a></li>
        <li><a href="/noticias">Noticias</a></li>
        <li><a href="/contacto">Contacto</a></li>
      </ul>
    </div>
    <div>
      <p class="text-[10px] uppercase tracking-[0.2em] text-[color:var(--color-ink-muted)] font-semibold">Legal</p>
      <ul class="mt-3 space-y-2">
        <li><a href="/legal/aviso-legal">Aviso legal</a></li>
        <li><a href="/legal/privacidad">Privacidad</a></li>
        <li><a href="/legal/cookies">Cookies</a></li>
      </ul>
      <p class="text-[10px] uppercase tracking-[0.2em] text-[color:var(--color-ink-muted)] font-semibold mt-6">Síguenos</p>
      <ul class="mt-3 space-y-2">
        <li><a href={site.social.instagram} rel="noopener" target="_blank">Instagram</a></li>
        <li><a href={site.social.facebook}  rel="noopener" target="_blank">Facebook</a></li>
      </ul>
    </div>
  </div>
  <div class="border-t border-[color:var(--color-line)]">
    <div class="mx-auto max-w-6xl px-4 sm:px-6 py-4 text-xs text-[color:var(--color-ink-muted)] flex flex-col sm:flex-row justify-between gap-2">
      <span>© {year} {site.name}. Todos los derechos reservados.</span>
      <span>Hecho con cariño en Madrid.</span>
    </div>
  </div>
</footer>
```

- [ ] **Step 4: Commit**

```bash
git add src/components/SkipLink.astro src/components/Header.astro src/components/Footer.astro
git commit -m "feat(components): Header (with CartBadge), Footer, SkipLink"
```

---

### Task 16: BaseLayout + MarketingLayout + LegalLayout

**Files:**

- Create: `src/layouts/BaseLayout.astro`
- Create: `src/layouts/MarketingLayout.astro`
- Create: `src/layouts/LegalLayout.astro`

- [ ] **Step 1: Write `src/layouts/BaseLayout.astro`**

```astro
---
import "~/styles/global.css";
import { ClientRouter } from "astro:transitions";
import SEO from "~/components/SEO.astro";
import SkipLink from "~/components/SkipLink.astro";
import ToastHost from "~/islands/ToastHost.tsx";
import { Analytics } from "@vercel/analytics/astro";
import { site } from "~/data/site";

interface Props {
  title: string;
  description: string;
  image?: string;
  canonical?: string;
  type?: "website" | "article" | "product";
  jsonLd?: Record<string, unknown> | Array<Record<string, unknown>>;
}

const props = Astro.props;
const orgJsonLd = {
  "@context": "https://schema.org",
  "@type": "Organization",
  name: site.name,
  url: site.url,
  logo: new URL("/favicon.svg", Astro.site ?? site.url).toString(),
  foundingDate: String(site.founded),
  sameAs: [site.social.instagram, site.social.facebook],
};
const jsonLd = props.jsonLd
  ? (Array.isArray(props.jsonLd) ? [orgJsonLd, ...props.jsonLd] : [orgJsonLd, props.jsonLd])
  : [orgJsonLd];
---
<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="theme-color" content="#f4ecdf" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <ClientRouter />
    <SEO
      title={props.title}
      description={props.description}
      image={props.image}
      canonical={props.canonical}
      type={props.type}
      jsonLd={jsonLd}
    />
  </head>
  <body>
    <SkipLink />
    <slot />
    <ToastHost client:load />
    <Analytics />
  </body>
</html>
```

- [ ] **Step 2: Write `src/layouts/MarketingLayout.astro`**

```astro
---
import BaseLayout from "~/layouts/BaseLayout.astro";
import Header from "~/components/Header.astro";
import Footer from "~/components/Footer.astro";
import CookieBanner from "~/islands/CookieBanner.tsx";

interface Props {
  title: string;
  description: string;
  image?: string;
  canonical?: string;
  type?: "website" | "article" | "product";
  jsonLd?: Record<string, unknown> | Array<Record<string, unknown>>;
}
const props = Astro.props;
---
<BaseLayout {...props}>
  <Header />
  <main id="main">
    <slot />
  </main>
  <Footer />
  <CookieBanner client:idle />
</BaseLayout>
```

- [ ] **Step 3: Write `src/layouts/LegalLayout.astro`**

```astro
---
import MarketingLayout from "~/layouts/MarketingLayout.astro";
interface Props {
  title: string;
  description: string;
}
const { title, description } = Astro.props;
---
<MarketingLayout title={title} description={description}>
  <section class="mx-auto max-w-3xl px-4 sm:px-6 py-12">
    <nav aria-label="Breadcrumb" class="text-xs text-[color:var(--color-ink-muted)] uppercase tracking-widest mb-4">
      <a href="/">Inicio</a> · <span>Legal</span> · <span>{title}</span>
    </nav>
    <h1 class="text-3xl sm:text-4xl mb-6">{title}</h1>
    <article class="prose prose-stone max-w-none [&_h2]:font-[family-name:var(--font-display)] [&_h2]:text-2xl [&_h2]:mt-8 [&_p]:my-4 [&_a]:underline">
      <slot />
    </article>
  </section>
</MarketingLayout>
```

- [ ] **Step 4: Commit**

```bash
git add src/layouts/
git commit -m "feat(layouts): Base/Marketing/Legal layouts with view transitions, SEO, toast host"
```

---

### Task 17: CookieBanner island

**Files:**

- Create: `src/islands/CookieBanner.tsx`

- [ ] **Step 1: Write `src/islands/CookieBanner.tsx`**

```tsx
import { useEffect, useState } from "react";

const KEY = "hsl-cookie-consent";
const CONSENT_EVENT = "hsl-cookie-consent-change";

type Consent = "accepted" | "rejected" | null;

export function getConsent(): Consent {
  if (typeof window === "undefined") return null;
  const v = window.localStorage.getItem(KEY);
  return v === "accepted" || v === "rejected" ? v : null;
}

export default function CookieBanner() {
  const [consent, setConsent] = useState<Consent>(null);
  const [mounted, setMounted] = useState(false);

  useEffect(() => {
    setConsent(getConsent());
    setMounted(true);
  }, []);

  function decide(value: "accepted" | "rejected") {
    window.localStorage.setItem(KEY, value);
    setConsent(value);
    window.dispatchEvent(new CustomEvent(CONSENT_EVENT, { detail: value }));
  }

  if (!mounted || consent !== null) return null;

  return (
    <div
      role="region"
      aria-label="Aviso de cookies"
      style={{
        position: "fixed",
        bottom: 0,
        left: 0,
        right: 0,
        background: "var(--color-ink)",
        color: "var(--color-cream)",
        padding: "1rem",
        zIndex: 70,
        boxShadow: "0 -8px 24px rgba(0,0,0,.15)",
      }}
    >
      <div
        style={{
          maxWidth: 960,
          margin: "0 auto",
          display: "flex",
          flexWrap: "wrap",
          gap: "0.75rem",
          alignItems: "center",
          justifyContent: "space-between",
          fontSize: 14,
        }}
      >
        <p style={{ margin: 0, maxWidth: "60ch" }}>
          Usamos cookies técnicas (carrito) y, si lo aceptas, mapas de Google.
          Las analíticas son anónimas.{" "}
          <a href="/legal/cookies" style={{ textDecoration: "underline" }}>
            Más info
          </a>
          .
        </p>
        <div style={{ display: "flex", gap: "0.5rem" }}>
          <button
            onClick={() => decide("rejected")}
            style={{
              border: "1px solid var(--color-cream)",
              background: "transparent",
              color: "var(--color-cream)",
              padding: "0.5rem 1rem",
              borderRadius: 9999,
              cursor: "pointer",
              fontWeight: 600,
            }}
          >
            Rechazar
          </button>
          <button
            onClick={() => decide("accepted")}
            style={{
              border: "none",
              background: "var(--color-cream)",
              color: "var(--color-ink)",
              padding: "0.5rem 1rem",
              borderRadius: 9999,
              cursor: "pointer",
              fontWeight: 700,
            }}
          >
            Aceptar
          </button>
        </div>
      </div>
    </div>
  );
}

export const COOKIE_CONSENT_EVENT = CONSENT_EVENT;
```

- [ ] **Step 2: Commit**

```bash
git add src/islands/CookieBanner.tsx
git commit -m "feat(islands): LSSI cookie banner with accept/reject"
```

---

## Phase 6 — Reusable components

### Task 18: Hero, SectionLabel, PromoBanner

**Files:**

- Create: `src/components/Hero.astro`
- Create: `src/components/SectionLabel.astro`
- Create: `src/components/PromoBanner.astro`

- [ ] **Step 1: Write `src/components/Hero.astro`**

```astro
---
import { Image } from "astro:assets";

interface Props {
  eyebrow?: string;
  title: string;
  italicTail?: string;
  body?: string;
  image: ImageMetadata | string;
  imageAlt: string;
  primaryCta?: { href: string; label: string };
  secondaryCta?: { href: string; label: string };
}
const { eyebrow, title, italicTail, body, image, imageAlt, primaryCta, secondaryCta } = Astro.props;
---
<section class="relative bg-[color:var(--color-cream)] overflow-hidden">
  <div class="mx-auto max-w-6xl px-4 sm:px-6 py-12 sm:py-20 grid gap-10 md:grid-cols-2 items-center">
    <div>
      {eyebrow && (
        <p class="text-[11px] uppercase tracking-[0.25em] text-[color:var(--color-ink-muted)] font-semibold mb-3">{eyebrow}</p>
      )}
      <h1 class="text-4xl sm:text-6xl lg:text-7xl leading-[0.95] text-[color:var(--color-ink)]">
        {title}
        {italicTail && <em class="italic text-[color:var(--color-accent-dark)] font-normal block">{italicTail}</em>}
      </h1>
      {body && <p class="mt-5 max-w-md text-[color:var(--color-ink-muted)] text-lg">{body}</p>}
      <div class="mt-7 flex flex-wrap gap-3">
        {primaryCta && (
          <a href={primaryCta.href} class="inline-flex items-center px-5 py-3 rounded-full bg-[color:var(--color-ink)] text-[color:var(--color-cream)] font-semibold hover:scale-[1.02] transition-transform">
            {primaryCta.label} →
          </a>
        )}
        {secondaryCta && (
          <a href={secondaryCta.href} class="inline-flex items-center px-5 py-3 rounded-full border border-[color:var(--color-ink)] text-[color:var(--color-ink)] font-semibold">
            {secondaryCta.label}
          </a>
        )}
      </div>
    </div>
    <div class="relative aspect-[4/3] md:aspect-[5/4] rounded-2xl overflow-hidden shadow-[var(--shadow-card)]">
      {typeof image === "string"
        ? <img src={image} alt={imageAlt} width="900" height="700" class="w-full h-full object-cover" />
        : <Image src={image} alt={imageAlt} widths={[480, 800, 1200]} sizes="(min-width:768px) 600px, 100vw" class="w-full h-full object-cover" />
      }
    </div>
  </div>
</section>
```

- [ ] **Step 2: Write `src/components/SectionLabel.astro`**

```astro
---
interface Props {
  number: string;
  label: string;
}
const { number, label } = Astro.props;
---
<p class="text-[11px] uppercase tracking-[0.25em] text-[color:var(--color-ink-muted)] font-semibold">
  Nº {number} — {label}
</p>
```

- [ ] **Step 3: Write `src/components/PromoBanner.astro`**

```astro
---
interface Props {
  label: string;
  title: string;
  ctaHref: string;
  ctaLabel: string;
}
const { label, title, ctaHref, ctaLabel } = Astro.props;
---
<section class="bg-[color:var(--color-hot)] text-white">
  <div class="mx-auto max-w-6xl px-4 sm:px-6 py-6 flex flex-wrap items-center justify-between gap-4">
    <div>
      <p class="text-[10px] uppercase tracking-[0.3em] opacity-85 font-semibold">{label}</p>
      <p class="font-[family-name:var(--font-display)] italic text-2xl sm:text-3xl mt-1">{title}</p>
    </div>
    <a href={ctaHref} class="bg-white text-[color:var(--color-hot)] px-5 py-2 rounded-full font-bold">{ctaLabel} →</a>
  </div>
</section>
```

- [ ] **Step 4: Commit**

```bash
git add src/components/Hero.astro src/components/SectionLabel.astro src/components/PromoBanner.astro
git commit -m "feat(components): Hero, SectionLabel, PromoBanner"
```

---

### Task 19: AddToCart island + ProductCard

**Files:**

- Create: `src/islands/AddToCart.tsx`
- Create: `src/components/ProductCard.astro`

- [ ] **Step 1: Write `src/islands/AddToCart.tsx`**

```tsx
import { useState } from "react";
import { addItem, type CartItem } from "~/lib/cart";
import { emitToast } from "~/islands/ToastHost";

type Variant = { id: string; label: string; priceCents: number };

type Props = {
  slug: string;
  name: string;
  unitPriceCents: number;
  variants?: Variant[];
  compact?: boolean;
};

export default function AddToCart({
  slug,
  name,
  unitPriceCents,
  variants,
  compact,
}: Props) {
  const [variantId, setVariantId] = useState<string | undefined>(
    variants?.[0]?.id,
  );
  const [qty, setQty] = useState(1);

  const variant = variants?.find((v) => v.id === variantId);
  const finalPrice = variant?.priceCents ?? unitPriceCents;

  function onAdd() {
    const item: Omit<CartItem, "qty"> & { qty: number } = {
      slug,
      name,
      variantId: variant?.id,
      variantLabel: variant?.label,
      unitPriceCents: finalPrice,
      qty,
    };
    addItem(item);
    emitToast(`Añadido — ${name}${variant ? ` (${variant.label})` : ""}`);
  }

  if (compact) {
    return (
      <button
        onClick={onAdd}
        style={{
          background: "var(--color-ink)",
          color: "var(--color-cream)",
          border: "none",
          padding: "0.5rem 1rem",
          borderRadius: 9999,
          fontWeight: 600,
          fontSize: 13,
          cursor: "pointer",
        }}
      >
        Añadir
      </button>
    );
  }

  return (
    <div style={{ display: "flex", flexDirection: "column", gap: "1rem" }}>
      {variants && variants.length > 1 && (
        <fieldset style={{ border: "none", padding: 0 }}>
          <legend
            style={{
              fontSize: 11,
              textTransform: "uppercase",
              letterSpacing: "0.18em",
              color: "var(--color-ink-muted)",
              fontWeight: 600,
              marginBottom: 8,
            }}
          >
            Tamaño / opción
          </legend>
          <div style={{ display: "flex", flexWrap: "wrap", gap: 8 }}>
            {variants.map((v) => (
              <label
                key={v.id}
                style={{
                  cursor: "pointer",
                  padding: "0.5rem 0.875rem",
                  border:
                    variantId === v.id
                      ? "2px solid var(--color-ink)"
                      : "1px solid var(--color-line)",
                  borderRadius: 9999,
                  background:
                    variantId === v.id ? "var(--color-cream)" : "white",
                  fontSize: 14,
                }}
              >
                <input
                  type="radio"
                  name="variant"
                  value={v.id}
                  checked={variantId === v.id}
                  onChange={() => setVariantId(v.id)}
                  style={{
                    position: "absolute",
                    opacity: 0,
                    pointerEvents: "none",
                  }}
                />
                {v.label}
              </label>
            ))}
          </div>
        </fieldset>
      )}

      <div style={{ display: "flex", alignItems: "center", gap: "1rem" }}>
        <label
          style={{
            fontSize: 11,
            textTransform: "uppercase",
            letterSpacing: "0.18em",
            color: "var(--color-ink-muted)",
            fontWeight: 600,
          }}
          htmlFor="qty"
        >
          Cantidad
        </label>
        <div
          style={{
            display: "inline-flex",
            alignItems: "center",
            border: "1px solid var(--color-line)",
            borderRadius: 9999,
          }}
        >
          <button
            type="button"
            onClick={() => setQty((q) => Math.max(1, q - 1))}
            aria-label="Reducir cantidad"
            style={{
              background: "transparent",
              border: "none",
              width: 36,
              height: 36,
              cursor: "pointer",
              fontSize: 16,
            }}
          >
            −
          </button>
          <span
            id="qty"
            style={{ minWidth: 28, textAlign: "center", fontWeight: 600 }}
          >
            {qty}
          </span>
          <button
            type="button"
            onClick={() => setQty((q) => Math.min(99, q + 1))}
            aria-label="Aumentar cantidad"
            style={{
              background: "transparent",
              border: "none",
              width: 36,
              height: 36,
              cursor: "pointer",
              fontSize: 16,
            }}
          >
            +
          </button>
        </div>
      </div>

      <button
        onClick={onAdd}
        style={{
          background: "var(--color-ink)",
          color: "var(--color-cream)",
          border: "none",
          padding: "0.875rem 1.5rem",
          borderRadius: 9999,
          fontSize: 15,
          fontWeight: 700,
          cursor: "pointer",
          transition: "transform .15s",
        }}
      >
        Añadir al pedido ·{" "}
        {new Intl.NumberFormat("es-ES", {
          style: "currency",
          currency: "EUR",
        }).format((finalPrice * qty) / 100)}
      </button>
    </div>
  );
}
```

- [ ] **Step 2: Write `src/components/ProductCard.astro`**

```astro
---
import { Image } from "astro:assets";
import AddToCart from "~/islands/AddToCart.tsx";
import { categoryById, type CategoryId } from "~/data/categories";
import { formatPriceCents } from "~/lib/format";

interface Props {
  slug: string;
  name: string;
  category: CategoryId;
  image: ImageMetadata;
  imageAlt: string;
  shortDescription: string;
  priceCents: number;
  unit?: string;
  variants?: { id: string; label: string; priceCents: number }[];
}

const { slug, name, category, image, imageAlt, shortDescription, priceCents, unit, variants } = Astro.props;
const cat = categoryById(category);
---
<article class="group bg-[color:var(--color-paper)] border border-[color:var(--color-line)] rounded-[var(--radius-card)] overflow-hidden shadow-[var(--shadow-card)] hover:scale-[1.02] transition-transform duration-200">
  <a href={`/catalogo/${slug}`} class="block">
    <div class="aspect-[4/3] overflow-hidden">
      <Image
        src={image}
        alt={imageAlt}
        widths={[300, 600, 900]}
        sizes="(min-width:1024px) 360px, (min-width:640px) 50vw, 100vw"
        class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
      />
    </div>
  </a>
  <div class="p-4 sm:p-5">
    <p class="text-[10px] uppercase tracking-[0.25em] text-[color:var(--color-accent-dark)] font-semibold">{cat.label}</p>
    <h3 class="font-[family-name:var(--font-display)] text-xl mt-1">
      <a href={`/catalogo/${slug}`} class="hover:text-[color:var(--color-accent-dark)]">{name}</a>
    </h3>
    <p class="text-sm text-[color:var(--color-ink-muted)] mt-1 line-clamp-2">{shortDescription}</p>
    <div class="flex items-center justify-between mt-4">
      <p class="font-[family-name:var(--font-display)] text-xl font-semibold">
        {formatPriceCents(priceCents)}{unit && <span class="text-xs text-[color:var(--color-ink-muted)] font-normal"> / {unit}</span>}
      </p>
      <AddToCart
        client:visible
        slug={slug}
        name={name}
        unitPriceCents={priceCents}
        variants={variants}
        compact={true}
      />
    </div>
  </div>
</article>
```

- [ ] **Step 3: Commit**

```bash
git add src/islands/AddToCart.tsx src/components/ProductCard.astro
git commit -m "feat(components): AddToCart island + ProductCard"
```

---

### Task 20: StoreCard, MapEmbed, ServiceCard, NewsCard

**Files:**

- Create: `src/components/MapEmbed.astro`
- Create: `src/components/StoreCard.astro`
- Create: `src/components/ServiceCard.astro`
- Create: `src/components/NewsCard.astro`

- [ ] **Step 1: Write `src/components/MapEmbed.astro`**

```astro
---
interface Props {
  embedUrl: string;
  externalUrl: string;
  title: string;
}
const { embedUrl, externalUrl, title } = Astro.props;
---
<div class="relative aspect-[4/3] sm:aspect-[16/9] rounded-[var(--radius-card)] overflow-hidden border border-[color:var(--color-line)] bg-[color:var(--color-cream)]" data-mapembed>
  <noscript>
    <a href={externalUrl} rel="noopener" target="_blank" class="absolute inset-0 grid place-items-center text-sm text-[color:var(--color-ink-muted)]">
      Ver mapa en Google Maps →
    </a>
  </noscript>
  <div class="absolute inset-0 grid place-items-center text-sm text-[color:var(--color-ink-muted)] text-center p-4" data-mapplaceholder>
    <p>
      Acepta cookies para ver el mapa,<br />
      o <a href={externalUrl} rel="noopener" target="_blank" class="underline">ábrelo en Google Maps →</a>
    </p>
  </div>
  <iframe
    title={`Mapa — ${title}`}
    src=""
    data-embed-url={embedUrl}
    width="100%"
    height="100%"
    loading="lazy"
    referrerpolicy="no-referrer-when-downgrade"
    class="absolute inset-0 w-full h-full hidden"
    data-mapframe
  ></iframe>
</div>

<script>
  function maybeLoadMaps() {
    const consent = localStorage.getItem("hsl-cookie-consent");
    if (consent !== "accepted") return;
    document.querySelectorAll<HTMLElement>("[data-mapembed]").forEach((wrap) => {
      const iframe = wrap.querySelector<HTMLIFrameElement>("[data-mapframe]");
      const placeholder = wrap.querySelector<HTMLElement>("[data-mapplaceholder]");
      if (!iframe || iframe.src) return;
      iframe.src = iframe.dataset.embedUrl ?? "";
      iframe.classList.remove("hidden");
      placeholder?.classList.add("hidden");
    });
  }
  maybeLoadMaps();
  window.addEventListener("hsl-cookie-consent-change", (e) => {
    if ((e as CustomEvent).detail === "accepted") maybeLoadMaps();
  });
</script>
```

- [ ] **Step 2: Write `src/components/StoreCard.astro`**

```astro
---
import { Image } from "astro:assets";
import MapEmbed from "~/components/MapEmbed.astro";
import type { Store } from "~/data/stores";

interface Props { store: Store; }
const { store } = Astro.props;
---
<article class="grid gap-6 md:grid-cols-2 items-start bg-[color:var(--color-paper)] border border-[color:var(--color-line)] rounded-[var(--radius-card)] p-5 sm:p-8 shadow-[var(--shadow-card)]">
  <div>
    <p class="text-[11px] uppercase tracking-[0.25em] text-[color:var(--color-ink-muted)] font-semibold">{store.shortName}</p>
    <h2 class="text-2xl sm:text-3xl mt-1">{store.name}</h2>
    <p class="text-[color:var(--color-ink-muted)] mt-3">{store.address}</p>
    <p class="text-[color:var(--color-ink-muted)]">{store.postal} {store.city}</p>

    <dl class="mt-5 grid gap-3 text-sm">
      <div>
        <dt class="text-[10px] uppercase tracking-[0.25em] text-[color:var(--color-ink-muted)] font-semibold">Horario</dt>
        <dd>{store.hoursText}</dd>
      </div>
      <div>
        <dt class="text-[10px] uppercase tracking-[0.25em] text-[color:var(--color-ink-muted)] font-semibold">Teléfono</dt>
        <dd><a href={`tel:${store.phone}`}>{store.phoneDisplay}</a></dd>
      </div>
      <div>
        <dt class="text-[10px] uppercase tracking-[0.25em] text-[color:var(--color-ink-muted)] font-semibold">Email</dt>
        <dd><a href={`mailto:${store.email}`}>{store.email}</a></dd>
      </div>
    </dl>

    <a href={store.mapsLink} rel="noopener" target="_blank" class="inline-flex items-center mt-6 px-5 py-2.5 rounded-full bg-[color:var(--color-ink)] text-[color:var(--color-cream)] font-semibold">
      Cómo llegar →
    </a>
  </div>
  <MapEmbed embedUrl={store.mapsEmbed} externalUrl={store.mapsLink} title={store.shortName} />
</article>
```

- [ ] **Step 3: Write `src/components/ServiceCard.astro`**

```astro
---
import type { Service } from "~/data/services";

interface Props { service: Service; }
const { service } = Astro.props;
const ICONS: Record<Service["icon"], string> = {
  truck: "🚚",
  party: "🎂",
  store: "🥖",
};
---
<article class="bg-[color:var(--color-paper)] border border-[color:var(--color-line)] rounded-[var(--radius-card)] p-6 shadow-[var(--shadow-card)]">
  <p class="text-3xl" aria-hidden="true">{ICONS[service.icon]}</p>
  <h3 class="font-[family-name:var(--font-display)] text-2xl mt-3">{service.title}</h3>
  <p class="text-[color:var(--color-ink-muted)] mt-2">{service.short}</p>
  <p class="text-sm mt-4">{service.body}</p>
</article>
```

- [ ] **Step 4: Write `src/components/NewsCard.astro`**

```astro
---
import { Image } from "astro:assets";
import { formatDate } from "~/lib/format";

interface Props {
  slug: string;
  title: string;
  excerpt: string;
  date: Date;
  image: ImageMetadata;
  imageAlt: string;
}
const { slug, title, excerpt, date, image, imageAlt } = Astro.props;
---
<article class="group bg-[color:var(--color-paper)] border border-[color:var(--color-line)] rounded-[var(--radius-card)] overflow-hidden shadow-[var(--shadow-card)]">
  <a href={`/noticias/${slug}`} class="block">
    <div class="aspect-[16/10] overflow-hidden">
      <Image
        src={image}
        alt={imageAlt}
        widths={[400, 800]}
        sizes="(min-width:768px) 400px, 100vw"
        class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
      />
    </div>
  </a>
  <div class="p-5">
    <p class="text-[10px] uppercase tracking-[0.25em] text-[color:var(--color-ink-muted)] font-semibold">{formatDate(date)}</p>
    <h3 class="font-[family-name:var(--font-display)] text-xl mt-1">
      <a href={`/noticias/${slug}`} class="hover:text-[color:var(--color-accent-dark)]">{title}</a>
    </h3>
    <p class="text-sm text-[color:var(--color-ink-muted)] mt-2 line-clamp-3">{excerpt}</p>
    <a href={`/noticias/${slug}`} class="inline-block mt-3 text-sm text-[color:var(--color-accent-dark)] font-semibold">Leer más →</a>
  </div>
</article>
```

- [ ] **Step 5: Commit**

```bash
git add src/components/MapEmbed.astro src/components/StoreCard.astro src/components/ServiceCard.astro src/components/NewsCard.astro
git commit -m "feat(components): StoreCard+MapEmbed (cookie-aware), ServiceCard, NewsCard"
```

---

## Phase 7 — Pages

### Task 21: Home page

**Files:**

- Modify: `src/pages/index.astro`

- [ ] **Step 1: Rewrite `src/pages/index.astro`**

```astro
---
import MarketingLayout from "~/layouts/MarketingLayout.astro";
import Hero from "~/components/Hero.astro";
import PromoBanner from "~/components/PromoBanner.astro";
import ProductCard from "~/components/ProductCard.astro";
import ServiceCard from "~/components/ServiceCard.astro";
import StoreCard from "~/components/StoreCard.astro";
import SectionLabel from "~/components/SectionLabel.astro";
import { getCollection } from "astro:content";
import { services } from "~/data/services";
import { stores } from "~/data/stores";
import { site } from "~/data/site";

const productsAll = await getCollection("products");
const featured = productsAll
  .filter((p) => p.data.featured)
  .sort((a, b) => a.data.order - b.data.order)
  .slice(0, 6);

const heroImage = (await import("../../public/images/hero/hero-pan.jpg")).default;

const localBizJsonLd = stores.map((s) => ({
  "@context": "https://schema.org",
  "@type": "Bakery",
  name: s.name,
  address: {
    "@type": "PostalAddress",
    streetAddress: s.address,
    addressLocality: s.city,
    postalCode: s.postal,
    addressCountry: "ES",
  },
  telephone: s.phone,
  geo: { "@type": "GeoCoordinates", latitude: s.coords.lat, longitude: s.coords.lng },
  openingHours: s.hoursText,
}));
---
<MarketingLayout
  title={site.name}
  description={site.tagline}
  jsonLd={localBizJsonLd}
>
  <Hero
    eyebrow="Desde 1986"
    title="Pan de pueblo,"
    italicTail="hecho a mano."
    body="Masa madre, fermentación lenta y harinas trazables. Lo de siempre, presentado con respeto."
    image={heroImage}
    imageAlt="Pan recién horneado sobre tabla de madera"
    primaryCta={{ href: "/catalogo", label: "Ver catálogo" }}
    secondaryCta={{ href: "/tiendas", label: "Nuestras tiendas" }}
  />

  <PromoBanner
    label="Temporada · diciembre"
    title="Roscón de Reyes — reservas abiertas"
    ctaHref="/catalogo/roscon-de-reyes"
    ctaLabel="Reservar"
  />

  <section class="mx-auto max-w-6xl px-4 sm:px-6 py-16 sm:py-24">
    <div class="flex items-end justify-between gap-4 mb-8">
      <div>
        <SectionLabel number="01" label="Lo que más se vende" />
        <h2 class="text-3xl sm:text-4xl mt-2">Nuestros imprescindibles</h2>
      </div>
      <a href="/catalogo" class="text-sm font-semibold underline">Ver todo el catálogo →</a>
    </div>
    <div class="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
      {featured.map((p) => (
        <ProductCard
          slug={p.id}
          name={p.data.name}
          category={p.data.category}
          image={p.data.image}
          imageAlt={p.data.imageAlt}
          shortDescription={p.data.shortDescription}
          priceCents={p.data.priceCents}
          unit={p.data.unit}
          variants={p.data.variants}
        />
      ))}
    </div>
  </section>

  <section class="bg-[color:var(--color-cream)]">
    <div class="mx-auto max-w-6xl px-4 sm:px-6 py-16 sm:py-24">
      <SectionLabel number="02" label="Lo que hacemos" />
      <h2 class="text-3xl sm:text-4xl mt-2">Servicios</h2>
      <p class="max-w-xl text-[color:var(--color-ink-muted)] mt-3">No solo vendemos en tienda — también llevamos pan a casa, atendemos eventos y servimos a otros restaurantes.</p>
      <div class="mt-8 grid gap-5 md:grid-cols-3">
        {services.map((s) => <ServiceCard service={s} />)}
      </div>
    </div>
  </section>

  <section class="mx-auto max-w-6xl px-4 sm:px-6 py-16 sm:py-24">
    <SectionLabel number="03" label="Dónde estamos" />
    <h2 class="text-3xl sm:text-4xl mt-2">Dos tiendas en Madrid</h2>
    <div class="mt-8 grid gap-8">
      {stores.map((s) => <StoreCard store={s} />)}
    </div>
  </section>
</MarketingLayout>
```

- [ ] **Step 2: Commit**

```bash
git add src/pages/index.astro
git commit -m "feat(pages): home with hero, promo, featured, services, tiendas"
```

---

### Task 22: Catalogo list + CategoryFilter island

**Files:**

- Create: `src/islands/CategoryFilter.tsx`
- Create: `src/pages/catalogo/index.astro`

- [ ] **Step 1: Write `src/islands/CategoryFilter.tsx`**

```tsx
import { useEffect, useState } from "react";
import { categories, type CategoryId } from "~/data/categories";

type Props = { current: CategoryId | "all" };

export default function CategoryFilter({ current }: Props) {
  const [active, setActive] = useState<CategoryId | "all">(current);

  useEffect(() => {
    const url = new URL(window.location.href);
    if (active === "all") url.searchParams.delete("cat");
    else url.searchParams.set("cat", active);
    window.history.replaceState({}, "", url.toString());

    document.querySelectorAll<HTMLElement>("[data-product]").forEach((el) => {
      const cat = el.dataset.category as CategoryId;
      el.style.display = active === "all" || cat === active ? "" : "none";
    });
  }, [active]);

  const all: Array<{ id: CategoryId | "all"; label: string }> = [
    { id: "all", label: "Todo" },
    ...categories.map((c) => ({ id: c.id, label: c.label })),
  ];

  return (
    <div
      role="tablist"
      aria-label="Filtrar por categoría"
      style={{ display: "flex", flexWrap: "wrap", gap: 8 }}
    >
      {all.map((c) => {
        const isActive = c.id === active;
        return (
          <button
            key={c.id}
            role="tab"
            aria-selected={isActive}
            onClick={() => setActive(c.id)}
            style={{
              padding: "0.5rem 1rem",
              borderRadius: 9999,
              border: isActive ? "none" : "1px solid var(--color-line)",
              background: isActive ? "var(--color-ink)" : "transparent",
              color: isActive ? "var(--color-cream)" : "var(--color-ink)",
              fontSize: 14,
              fontWeight: 600,
              cursor: "pointer",
            }}
          >
            {c.label}
          </button>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 2: Write `src/pages/catalogo/index.astro`**

```astro
---
import MarketingLayout from "~/layouts/MarketingLayout.astro";
import ProductCard from "~/components/ProductCard.astro";
import CategoryFilter from "~/islands/CategoryFilter.tsx";
import { getCollection } from "astro:content";
import { categoryIds, type CategoryId } from "~/data/categories";

const url = Astro.url;
const catParam = url.searchParams.get("cat") as CategoryId | null;
const initial: CategoryId | "all" = catParam && (categoryIds as readonly string[]).includes(catParam) ? catParam : "all";

const products = (await getCollection("products"))
  .sort((a, b) => a.data.order - b.data.order);
---
<MarketingLayout
  title="Catálogo"
  description="Panes, bollería, tartas, salado y temporada. Elige y pídelo por WhatsApp."
>
  <section class="mx-auto max-w-6xl px-4 sm:px-6 py-12">
    <p class="text-[11px] uppercase tracking-[0.25em] text-[color:var(--color-ink-muted)] font-semibold">Catálogo</p>
    <h1 class="text-4xl sm:text-5xl mt-2">Todo lo que sale del horno.</h1>
    <p class="text-[color:var(--color-ink-muted)] mt-3 max-w-xl">Filtra por categoría, añade al pedido y termínalo por WhatsApp.</p>

    <div class="mt-8">
      <CategoryFilter client:visible current={initial} />
    </div>

    <div class="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
      {products.map((p) => (
        <div data-product data-category={p.data.category}>
          <ProductCard
            slug={p.id}
            name={p.data.name}
            category={p.data.category}
            image={p.data.image}
            imageAlt={p.data.imageAlt}
            shortDescription={p.data.shortDescription}
            priceCents={p.data.priceCents}
            unit={p.data.unit}
            variants={p.data.variants}
          />
        </div>
      ))}
    </div>
  </section>
</MarketingLayout>
```

- [ ] **Step 3: Commit**

```bash
git add src/islands/CategoryFilter.tsx src/pages/catalogo/index.astro
git commit -m "feat(pages): /catalogo with CategoryFilter island"
```

---

### Task 23: Product detail page

**Files:**

- Create: `src/pages/catalogo/[slug].astro`

- [ ] **Step 1: Write `src/pages/catalogo/[slug].astro`**

```astro
---
import { Image } from "astro:assets";
import { getCollection, render } from "astro:content";
import MarketingLayout from "~/layouts/MarketingLayout.astro";
import AddToCart from "~/islands/AddToCart.tsx";
import { categoryById } from "~/data/categories";
import { formatPriceCents } from "~/lib/format";

export async function getStaticPaths() {
  const products = await getCollection("products");
  return products.map((p) => ({ params: { slug: p.id }, props: { product: p } }));
}

const { product } = Astro.props;
const { Content } = await render(product);
const cat = categoryById(product.data.category);

const allergensLabel: Record<string, string> = {
  gluten: "gluten",
  huevo: "huevo",
  leche: "leche",
  "frutos-secos": "frutos secos",
  soja: "soja",
  sesamo: "sésamo",
  sulfitos: "sulfitos",
};

const jsonLd = {
  "@context": "https://schema.org",
  "@type": "Product",
  name: product.data.name,
  description: product.data.shortDescription,
  image: new URL(product.data.image.src, Astro.site).toString(),
  category: cat.label,
  offers: {
    "@type": "Offer",
    priceCurrency: "EUR",
    price: (product.data.priceCents / 100).toFixed(2),
    availability: "https://schema.org/InStock",
  },
};

const breadcrumbs = {
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  itemListElement: [
    { "@type": "ListItem", position: 1, name: "Inicio", item: new URL("/", Astro.site).toString() },
    { "@type": "ListItem", position: 2, name: "Catálogo", item: new URL("/catalogo", Astro.site).toString() },
    { "@type": "ListItem", position: 3, name: product.data.name },
  ],
};
---
<MarketingLayout
  title={product.data.name}
  description={product.data.shortDescription}
  type="product"
  jsonLd={[jsonLd, breadcrumbs]}
>
  <section class="mx-auto max-w-6xl px-4 sm:px-6 py-10">
    <nav aria-label="Breadcrumb" class="text-xs text-[color:var(--color-ink-muted)] uppercase tracking-widest mb-6">
      <a href="/">Inicio</a> · <a href="/catalogo">Catálogo</a> · <span>{product.data.name}</span>
    </nav>

    <div class="grid gap-10 md:grid-cols-2">
      <div class="rounded-[var(--radius-card)] overflow-hidden border border-[color:var(--color-line)]">
        <Image
          src={product.data.image}
          alt={product.data.imageAlt}
          widths={[600, 900, 1200]}
          sizes="(min-width:768px) 600px, 100vw"
          class="w-full h-auto"
        />
      </div>

      <div>
        <p class="text-[10px] uppercase tracking-[0.25em] text-[color:var(--color-accent-dark)] font-semibold">{cat.label}</p>
        <h1 class="text-4xl sm:text-5xl mt-2">{product.data.name}</h1>
        <p class="text-lg text-[color:var(--color-ink-muted)] mt-3">{product.data.shortDescription}</p>

        <p class="font-[family-name:var(--font-display)] text-3xl mt-6">
          {formatPriceCents(product.data.priceCents)}
          {product.data.unit && <span class="text-sm text-[color:var(--color-ink-muted)] font-normal"> / {product.data.unit}</span>}
        </p>

        <div class="mt-6 max-w-md">
          <AddToCart
            client:load
            slug={product.id}
            name={product.data.name}
            unitPriceCents={product.data.priceCents}
            variants={product.data.variants}
          />
        </div>

        {product.data.allergens.length > 0 && (
          <div class="mt-8">
            <p class="text-[10px] uppercase tracking-[0.25em] text-[color:var(--color-ink-muted)] font-semibold">Alérgenos</p>
            <p class="mt-1 text-sm">Contiene: {product.data.allergens.map((a) => allergensLabel[a]).join(", ")}.</p>
          </div>
        )}

        <article class="mt-8 prose prose-stone max-w-none [&_p]:my-3 text-[color:var(--color-ink)]">
          <Content />
        </article>
      </div>
    </div>
  </section>
</MarketingLayout>
```

- [ ] **Step 2: Commit**

```bash
git add src/pages/catalogo/[slug].astro
git commit -m "feat(pages): product detail with AddToCart, Product+Breadcrumb JSON-LD"
```

---

### Task 24: Carrito page + CartPage island

**Files:**

- Create: `src/islands/CartPage.tsx`
- Create: `src/pages/carrito.astro`

- [ ] **Step 1: Write `src/islands/CartPage.tsx`**

```tsx
import { useMemo, useState } from "react";
import { useCart } from "~/hooks/useCart";
import { stores, type StoreId } from "~/data/stores";
import {
  buildWhatsAppMessage,
  buildWhatsAppUrl,
  type PickupInfo,
} from "~/lib/whatsapp";
import { formatPriceCents } from "~/lib/format";

const PHONE = import.meta.env.PUBLIC_WHATSAPP_NUMBER;

function nextNDays(start: Date, n: number) {
  const days: string[] = [];
  const d = new Date(start);
  d.setDate(d.getDate() + 1);
  for (let i = 0; i < n; i++) {
    days.push(d.toISOString().slice(0, 10));
    d.setDate(d.getDate() + 1);
  }
  return days;
}

export default function CartPage() {
  const { cart, totalCents, totalQty, updateQty, removeItem, clearCart } =
    useCart();

  const [storeId, setStoreId] = useState<StoreId>("alcobendas");
  const [dateISO, setDateISO] = useState<string>("");
  const [slot, setSlot] = useState<PickupInfo["slot"]>("morning");
  const [name, setName] = useState<string>("");
  const [notes, setNotes] = useState<string>("");

  const store = stores.find((s) => s.id === storeId)!;
  const availableDates = useMemo(() => {
    const days = nextNDays(new Date(), 7);
    return days.filter((d) => {
      const dow = new Date(d + "T00:00:00").getDay();
      return store.openDays.includes(dow);
    });
  }, [store]);

  const canSubmit = cart.items.length > 0 && dateISO !== "";

  function submit() {
    if (!canSubmit) return;
    const pickup: PickupInfo = {
      storeId,
      dateISO,
      slot,
      name: name.trim() || undefined,
      notes: notes.trim() || undefined,
    };
    const msg = buildWhatsAppMessage(cart, pickup);
    const url = buildWhatsAppUrl(PHONE, msg);
    window.location.href = url;
  }

  if (cart.items.length === 0) {
    return (
      <section
        style={{
          maxWidth: 720,
          margin: "0 auto",
          padding: "3rem 1rem",
          textAlign: "center",
        }}
      >
        <h1
          style={{
            fontFamily: "var(--font-display)",
            fontSize: 36,
            marginBottom: 12,
          }}
        >
          Tu pedido está vacío
        </h1>
        <p style={{ color: "var(--color-ink-muted)", marginBottom: 24 }}>
          Empieza por el catálogo y añade lo que necesites.
        </p>
        <a
          href="/catalogo"
          style={{
            display: "inline-block",
            background: "var(--color-ink)",
            color: "var(--color-cream)",
            padding: "0.875rem 1.5rem",
            borderRadius: 9999,
            fontWeight: 700,
          }}
        >
          Ver catálogo →
        </a>
      </section>
    );
  }

  return (
    <section style={{ maxWidth: 960, margin: "0 auto", padding: "2rem 1rem" }}>
      <p
        style={{
          fontSize: 11,
          textTransform: "uppercase",
          letterSpacing: "0.25em",
          color: "var(--color-ink-muted)",
          fontWeight: 600,
        }}
      >
        Tu pedido
      </p>
      <h1
        style={{
          fontFamily: "var(--font-display)",
          fontSize: 40,
          marginTop: 4,
        }}
      >
        {totalQty} {totalQty === 1 ? "artículo" : "artículos"}
      </h1>

      <div style={{ display: "grid", gap: 12, marginTop: 24 }}>
        {cart.items.map((item) => (
          <div
            key={item.slug + (item.variantId ?? "")}
            style={{
              display: "grid",
              gridTemplateColumns: "1fr auto",
              gap: 16,
              padding: 16,
              border: "1px solid var(--color-line)",
              borderRadius: 12,
              background: "var(--color-paper)",
            }}
          >
            <div>
              <p style={{ fontFamily: "var(--font-display)", fontSize: 20 }}>
                {item.name}
              </p>
              {item.variantLabel && (
                <p style={{ fontSize: 13, color: "var(--color-ink-muted)" }}>
                  {item.variantLabel}
                </p>
              )}
              <p
                style={{
                  fontSize: 14,
                  color: "var(--color-ink-muted)",
                  marginTop: 4,
                }}
              >
                {formatPriceCents(item.unitPriceCents)} c/u
              </p>
            </div>
            <div
              style={{
                display: "flex",
                flexDirection: "column",
                alignItems: "flex-end",
                gap: 8,
              }}
            >
              <div
                style={{
                  display: "inline-flex",
                  alignItems: "center",
                  border: "1px solid var(--color-line)",
                  borderRadius: 9999,
                }}
              >
                <button
                  onClick={() =>
                    updateQty(item.slug, item.variantId, item.qty - 1)
                  }
                  aria-label="−"
                  style={{
                    width: 32,
                    height: 32,
                    background: "transparent",
                    border: "none",
                    cursor: "pointer",
                  }}
                >
                  −
                </button>
                <span
                  style={{ minWidth: 24, textAlign: "center", fontWeight: 600 }}
                >
                  {item.qty}
                </span>
                <button
                  onClick={() =>
                    updateQty(item.slug, item.variantId, item.qty + 1)
                  }
                  aria-label="+"
                  style={{
                    width: 32,
                    height: 32,
                    background: "transparent",
                    border: "none",
                    cursor: "pointer",
                  }}
                >
                  +
                </button>
              </div>
              <p
                style={{
                  fontFamily: "var(--font-display)",
                  fontSize: 18,
                  fontWeight: 600,
                }}
              >
                {formatPriceCents(item.unitPriceCents * item.qty)}
              </p>
              <button
                onClick={() => removeItem(item.slug, item.variantId)}
                style={{
                  background: "transparent",
                  border: "none",
                  color: "var(--color-ink-muted)",
                  fontSize: 12,
                  textDecoration: "underline",
                  cursor: "pointer",
                }}
              >
                Quitar
              </button>
            </div>
          </div>
        ))}
      </div>

      <div
        style={{
          marginTop: 24,
          display: "flex",
          justifyContent: "space-between",
          fontSize: 18,
        }}
      >
        <span>Total estimado</span>
        <span
          style={{
            fontFamily: "var(--font-display)",
            fontWeight: 700,
            fontSize: 24,
          }}
        >
          {formatPriceCents(totalCents)}
        </span>
      </div>

      <hr
        style={{
          margin: "32px 0",
          border: "none",
          borderTop: "1px solid var(--color-line)",
        }}
      />

      <h2 style={{ fontFamily: "var(--font-display)", fontSize: 28 }}>
        Recogida
      </h2>

      <fieldset style={{ border: "none", padding: 0, marginTop: 16 }}>
        <legend
          style={{
            fontSize: 11,
            textTransform: "uppercase",
            letterSpacing: "0.25em",
            color: "var(--color-ink-muted)",
            fontWeight: 600,
          }}
        >
          📍 Tienda
        </legend>
        <div
          style={{ display: "flex", flexWrap: "wrap", gap: 8, marginTop: 8 }}
        >
          {stores.map((s) => (
            <label
              key={s.id}
              style={{
                padding: "0.75rem 1rem",
                border:
                  storeId === s.id
                    ? "2px solid var(--color-ink)"
                    : "1px solid var(--color-line)",
                borderRadius: 12,
                cursor: "pointer",
                background: storeId === s.id ? "var(--color-cream)" : "white",
              }}
            >
              <input
                type="radio"
                name="store"
                value={s.id}
                checked={storeId === s.id}
                onChange={() => {
                  setStoreId(s.id);
                  setDateISO("");
                }}
                style={{ position: "absolute", opacity: 0 }}
              />
              <strong style={{ display: "block" }}>{s.shortName}</strong>
              <span style={{ fontSize: 12, color: "var(--color-ink-muted)" }}>
                {s.hoursText}
              </span>
            </label>
          ))}
        </div>
      </fieldset>

      <div style={{ marginTop: 24 }}>
        <label
          htmlFor="pickup-date"
          style={{
            fontSize: 11,
            textTransform: "uppercase",
            letterSpacing: "0.25em",
            color: "var(--color-ink-muted)",
            fontWeight: 600,
          }}
        >
          📅 Día
        </label>
        <select
          id="pickup-date"
          value={dateISO}
          onChange={(e) => setDateISO(e.target.value)}
          style={{
            display: "block",
            marginTop: 8,
            padding: "0.75rem 1rem",
            border: "1px solid var(--color-line)",
            borderRadius: 9999,
            background: "white",
            fontSize: 14,
            width: "100%",
            maxWidth: 320,
          }}
        >
          <option value="">Elige un día</option>
          {availableDates.map((d) => (
            <option key={d} value={d}>
              {new Intl.DateTimeFormat("es-ES", {
                weekday: "long",
                day: "2-digit",
                month: "long",
              }).format(new Date(d + "T00:00:00"))}
            </option>
          ))}
        </select>
      </div>

      <fieldset style={{ border: "none", padding: 0, marginTop: 24 }}>
        <legend
          style={{
            fontSize: 11,
            textTransform: "uppercase",
            letterSpacing: "0.25em",
            color: "var(--color-ink-muted)",
            fontWeight: 600,
          }}
        >
          🕘 Franja
        </legend>
        <div style={{ display: "flex", gap: 8, marginTop: 8 }}>
          {[
            { id: "morning", label: "Mañana" },
            { id: "afternoon", label: "Tarde" },
          ].map((s) => (
            <label
              key={s.id}
              style={{
                padding: "0.5rem 1rem",
                border:
                  slot === s.id
                    ? "2px solid var(--color-ink)"
                    : "1px solid var(--color-line)",
                borderRadius: 9999,
                cursor: "pointer",
                background: slot === s.id ? "var(--color-cream)" : "white",
                fontSize: 14,
              }}
            >
              <input
                type="radio"
                name="slot"
                value={s.id}
                checked={slot === s.id}
                onChange={() => setSlot(s.id as PickupInfo["slot"])}
                style={{ position: "absolute", opacity: 0 }}
              />
              {s.label}
            </label>
          ))}
        </div>
      </fieldset>

      <div style={{ marginTop: 24 }}>
        <label
          htmlFor="pickup-name"
          style={{
            fontSize: 11,
            textTransform: "uppercase",
            letterSpacing: "0.25em",
            color: "var(--color-ink-muted)",
            fontWeight: 600,
          }}
        >
          👤 Tu nombre (opcional)
        </label>
        <input
          id="pickup-name"
          value={name}
          onChange={(e) => setName(e.target.value)}
          placeholder="Para que sepan quién pregunta"
          style={{
            display: "block",
            marginTop: 8,
            padding: "0.75rem 1rem",
            border: "1px solid var(--color-line)",
            borderRadius: 9999,
            width: "100%",
            maxWidth: 320,
            fontSize: 14,
          }}
        />
      </div>

      <div style={{ marginTop: 16 }}>
        <label
          htmlFor="pickup-notes"
          style={{
            fontSize: 11,
            textTransform: "uppercase",
            letterSpacing: "0.25em",
            color: "var(--color-ink-muted)",
            fontWeight: 600,
          }}
        >
          📝 Notas (opcional)
        </label>
        <textarea
          id="pickup-notes"
          value={notes}
          onChange={(e) => setNotes(e.target.value)}
          rows={3}
          placeholder="Alergias, indicaciones, lo que necesites"
          style={{
            display: "block",
            marginTop: 8,
            padding: "0.75rem 1rem",
            border: "1px solid var(--color-line)",
            borderRadius: 12,
            width: "100%",
            maxWidth: 480,
            fontSize: 14,
            fontFamily: "inherit",
          }}
        />
      </div>

      <div
        style={{
          position: "sticky",
          bottom: 16,
          marginTop: 32,
          background: "var(--color-paper)",
          padding: 16,
          border: "1px solid var(--color-line)",
          borderRadius: 16,
        }}
      >
        <button
          onClick={submit}
          disabled={!canSubmit}
          style={{
            width: "100%",
            background: canSubmit
              ? "var(--color-wa)"
              : "var(--color-ink-muted)",
            color: "white",
            border: "none",
            padding: "1rem 1.5rem",
            borderRadius: 9999,
            fontSize: 16,
            fontWeight: 700,
            cursor: canSubmit ? "pointer" : "not-allowed",
            display: "inline-flex",
            alignItems: "center",
            justifyContent: "center",
            gap: 8,
          }}
        >
          ✓ Pedir por WhatsApp · {formatPriceCents(totalCents)}
        </button>
        <p
          style={{
            marginTop: 8,
            fontSize: 12,
            color: "var(--color-ink-muted)",
            fontStyle: "italic",
            textAlign: "center",
          }}
        >
          Al pulsar se abre WhatsApp con el pedido escrito. El horno te confirma
          disponibilidad, precio final y hora exacta antes de prepararlo. Aún no
          estás comprando.
        </p>
      </div>

      <p style={{ textAlign: "center", marginTop: 16 }}>
        <button
          onClick={() => {
            if (confirm("¿Vaciar el carrito?")) clearCart();
          }}
          style={{
            background: "transparent",
            border: "none",
            color: "var(--color-ink-muted)",
            textDecoration: "underline",
            cursor: "pointer",
            fontSize: 13,
          }}
        >
          Vaciar carrito
        </button>
      </p>
    </section>
  );
}
```

- [ ] **Step 2: Write `src/pages/carrito.astro`**

```astro
---
import MarketingLayout from "~/layouts/MarketingLayout.astro";
import CartPage from "~/islands/CartPage.tsx";
---
<MarketingLayout
  title="Tu pedido"
  description="Revisa tu pedido y termínalo por WhatsApp."
>
  <CartPage client:only="react" />
</MarketingLayout>
```

- [ ] **Step 3: Commit**

```bash
git add src/islands/CartPage.tsx src/pages/carrito.astro
git commit -m "feat(pages): /carrito with full pickup + WhatsApp flow"
```

---

### Task 25: Servicios, Tradición, Tiendas pages

**Files:**

- Create: `src/pages/servicios.astro`
- Create: `src/pages/tradicion-y-calidad.astro`
- Create: `src/pages/tiendas.astro`

- [ ] **Step 1: Write `src/pages/servicios.astro`**

```astro
---
import MarketingLayout from "~/layouts/MarketingLayout.astro";
import ServiceCard from "~/components/ServiceCard.astro";
import { services } from "~/data/services";
---
<MarketingLayout
  title="Servicios"
  description="A domicilio, catering y distribución a hostelería. Atendemos a particulares y empresas."
>
  <section class="mx-auto max-w-6xl px-4 sm:px-6 py-12 sm:py-16">
    <p class="text-[11px] uppercase tracking-[0.25em] text-[color:var(--color-ink-muted)] font-semibold">Servicios</p>
    <h1 class="text-4xl sm:text-5xl mt-2">Lo que ofrecemos.</h1>
    <p class="text-[color:var(--color-ink-muted)] mt-3 max-w-2xl">Servimos a particulares y a empresas: a domicilio, catering para eventos y distribución profesional a la hostelería madrileña.</p>

    <div class="mt-10 grid gap-6 md:grid-cols-3">
      {services.map((s) => <ServiceCard service={s} />)}
    </div>

    <div class="mt-16 bg-[color:var(--color-cream)] rounded-[var(--radius-card)] p-8 sm:p-12 text-center">
      <h2 class="text-3xl sm:text-4xl">¿Necesitas un presupuesto?</h2>
      <p class="text-[color:var(--color-ink-muted)] mt-3 max-w-xl mx-auto">Cuéntanos qué tienes en mente y te respondemos en menos de 24 horas.</p>
      <a href="/contacto" class="inline-block mt-6 bg-[color:var(--color-ink)] text-[color:var(--color-cream)] px-6 py-3 rounded-full font-semibold">Hablamos →</a>
    </div>
  </section>
</MarketingLayout>
```

- [ ] **Step 2: Write `src/pages/tradicion-y-calidad.astro`**

```astro
---
import MarketingLayout from "~/layouts/MarketingLayout.astro";
import { tradicion } from "~/data/tradicion";
---
<MarketingLayout
  title="Tradición y Calidad"
  description="Desde 1986: masa madre, fermentación lenta, harinas trazables. La calidad no viene de un sobre."
>
  <section class="mx-auto max-w-3xl px-4 sm:px-6 py-12 sm:py-16">
    <p class="text-[11px] uppercase tracking-[0.25em] text-[color:var(--color-ink-muted)] font-semibold">Tradición y calidad</p>
    <h1 class="text-4xl sm:text-5xl mt-2">Desde 1986, sin atajos.</h1>
    <p class="text-xl text-[color:var(--color-ink-muted)] mt-6 leading-relaxed">{tradicion.intro}</p>

    <div class="mt-12 grid gap-8 sm:grid-cols-2">
      {tradicion.pillars.map((p) => (
        <article>
          <h2 class="text-2xl">{p.title}</h2>
          <p class="text-[color:var(--color-ink-muted)] mt-2 leading-relaxed">{p.body}</p>
        </article>
      ))}
    </div>

    <div class="mt-16 bg-[color:var(--color-cream)] rounded-[var(--radius-card)] p-8">
      <p class="text-[11px] uppercase tracking-[0.25em] text-[color:var(--color-ink-muted)] font-semibold">Certificaciones</p>
      <ul class="mt-3 space-y-2 text-sm">
        {tradicion.certifications.map((c) => <li>· {c}</li>)}
      </ul>
    </div>
  </section>
</MarketingLayout>
```

- [ ] **Step 3: Write `src/pages/tiendas.astro`**

```astro
---
import MarketingLayout from "~/layouts/MarketingLayout.astro";
import StoreCard from "~/components/StoreCard.astro";
import { stores } from "~/data/stores";

const jsonLd = stores.map((s) => ({
  "@context": "https://schema.org",
  "@type": "Bakery",
  name: s.name,
  address: {
    "@type": "PostalAddress",
    streetAddress: s.address,
    addressLocality: s.city,
    postalCode: s.postal,
    addressCountry: "ES",
  },
  telephone: s.phone,
  email: s.email,
  geo: { "@type": "GeoCoordinates", latitude: s.coords.lat, longitude: s.coords.lng },
  openingHours: s.hoursText,
}));
---
<MarketingLayout
  title="Tiendas"
  description="Dos tiendas en Madrid: Alcobendas (central) y Pozuelo de Alarcón."
  jsonLd={jsonLd}
>
  <section class="mx-auto max-w-6xl px-4 sm:px-6 py-12">
    <p class="text-[11px] uppercase tracking-[0.25em] text-[color:var(--color-ink-muted)] font-semibold">Tiendas</p>
    <h1 class="text-4xl sm:text-5xl mt-2">Dos sitios donde encontrarnos.</h1>
    <div class="mt-10 grid gap-8">
      {stores.map((s) => <StoreCard store={s} />)}
    </div>
  </section>
</MarketingLayout>
```

- [ ] **Step 4: Commit**

```bash
git add src/pages/servicios.astro src/pages/tradicion-y-calidad.astro src/pages/tiendas.astro
git commit -m "feat(pages): servicios, tradicion-y-calidad, tiendas"
```

---

### Task 26: Noticias list + detail pages

**Files:**

- Create: `src/pages/noticias/index.astro`
- Create: `src/pages/noticias/[slug].astro`

- [ ] **Step 1: Write `src/pages/noticias/index.astro`**

```astro
---
import MarketingLayout from "~/layouts/MarketingLayout.astro";
import NewsCard from "~/components/NewsCard.astro";
import { getCollection } from "astro:content";

const items = (await getCollection("noticias", ({ data }) => !data.draft))
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime());
---
<MarketingLayout
  title="Noticias y promociones"
  description="Roscón de Reyes, San Valentín, Día del Padre, fiestas patronales y más."
>
  <section class="mx-auto max-w-6xl px-4 sm:px-6 py-12">
    <p class="text-[11px] uppercase tracking-[0.25em] text-[color:var(--color-ink-muted)] font-semibold">Noticias</p>
    <h1 class="text-4xl sm:text-5xl mt-2">Lo que tenemos esta temporada.</h1>
    <div class="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
      {items.map((n) => (
        <NewsCard
          slug={n.id}
          title={n.data.title}
          excerpt={n.data.excerpt}
          date={n.data.date}
          image={n.data.image}
          imageAlt={n.data.imageAlt}
        />
      ))}
    </div>
  </section>
</MarketingLayout>
```

- [ ] **Step 2: Write `src/pages/noticias/[slug].astro`**

```astro
---
import { Image } from "astro:assets";
import { getCollection, render } from "astro:content";
import MarketingLayout from "~/layouts/MarketingLayout.astro";
import { formatDate } from "~/lib/format";
import { site } from "~/data/site";

export async function getStaticPaths() {
  const items = await getCollection("noticias", ({ data }) => !data.draft);
  return items.map((n) => ({ params: { slug: n.id }, props: { item: n } }));
}

const { item } = Astro.props;
const { Content } = await render(item);

const jsonLd = {
  "@context": "https://schema.org",
  "@type": "Article",
  headline: item.data.title,
  datePublished: item.data.date.toISOString(),
  image: new URL(item.data.image.src, Astro.site).toString(),
  publisher: {
    "@type": "Organization",
    name: site.name,
    logo: { "@type": "ImageObject", url: new URL("/favicon.svg", Astro.site).toString() },
  },
};
---
<MarketingLayout
  title={item.data.title}
  description={item.data.excerpt}
  type="article"
  jsonLd={jsonLd}
>
  <article class="mx-auto max-w-3xl px-4 sm:px-6 py-12">
    <nav aria-label="Breadcrumb" class="text-xs text-[color:var(--color-ink-muted)] uppercase tracking-widest mb-4">
      <a href="/">Inicio</a> · <a href="/noticias">Noticias</a> · <span>{item.data.title}</span>
    </nav>

    <p class="text-[10px] uppercase tracking-[0.25em] text-[color:var(--color-ink-muted)] font-semibold">{formatDate(item.data.date)}</p>
    <h1 class="text-4xl sm:text-5xl mt-2">{item.data.title}</h1>
    <p class="text-lg text-[color:var(--color-ink-muted)] mt-4">{item.data.excerpt}</p>

    <div class="mt-8 rounded-[var(--radius-card)] overflow-hidden border border-[color:var(--color-line)]">
      <Image src={item.data.image} alt={item.data.imageAlt} widths={[600, 900, 1200]} sizes="(min-width:768px) 720px, 100vw" class="w-full h-auto" />
    </div>

    <div class="prose prose-stone mt-8 max-w-none [&_p]:my-4 text-[color:var(--color-ink)]">
      <Content />
    </div>
  </article>
</MarketingLayout>
```

- [ ] **Step 3: Commit**

```bash
git add src/pages/noticias/
git commit -m "feat(pages): noticias list + detail with Article JSON-LD"
```

---

### Task 27: Contacto page + ContactForm island

**Files:**

- Create: `src/islands/ContactForm.tsx`
- Create: `src/pages/contacto.astro`

- [ ] **Step 1: Write `src/islands/ContactForm.tsx`**

```tsx
import { useState } from "react";
import { site } from "~/data/site";

export default function ContactForm() {
  const [name, setName] = useState("");
  const [email, setEmail] = useState("");
  const [phone, setPhone] = useState("");
  const [message, setMessage] = useState("");
  const [accepted, setAccepted] = useState(false);

  const canSend =
    name.trim() &&
    email.includes("@") &&
    message.trim().length >= 10 &&
    accepted;

  function send(e: React.FormEvent) {
    e.preventDefault();
    if (!canSend) return;
    const body = `Nombre: ${name}\nEmail: ${email}\nTeléfono: ${phone}\n\n${message}`;
    const subject = `Consulta desde la web — ${name}`;
    window.location.href = `mailto:${site.email}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;
  }

  const inputStyle: React.CSSProperties = {
    display: "block",
    width: "100%",
    padding: "0.75rem 1rem",
    border: "1px solid var(--color-line)",
    borderRadius: 12,
    fontSize: 14,
    fontFamily: "inherit",
    marginTop: 6,
  };

  const labelStyle: React.CSSProperties = {
    fontSize: 11,
    textTransform: "uppercase",
    letterSpacing: "0.18em",
    color: "var(--color-ink-muted)",
    fontWeight: 600,
  };

  return (
    <form onSubmit={send} style={{ display: "grid", gap: 16, maxWidth: 540 }}>
      <div>
        <label style={labelStyle} htmlFor="cn-name">
          Nombre *
        </label>
        <input
          id="cn-name"
          required
          value={name}
          onChange={(e) => setName(e.target.value)}
          style={inputStyle}
        />
      </div>
      <div>
        <label style={labelStyle} htmlFor="cn-email">
          Email *
        </label>
        <input
          id="cn-email"
          required
          type="email"
          value={email}
          onChange={(e) => setEmail(e.target.value)}
          style={inputStyle}
        />
      </div>
      <div>
        <label style={labelStyle} htmlFor="cn-phone">
          Teléfono
        </label>
        <input
          id="cn-phone"
          value={phone}
          onChange={(e) => setPhone(e.target.value)}
          style={inputStyle}
        />
      </div>
      <div>
        <label style={labelStyle} htmlFor="cn-msg">
          Mensaje *
        </label>
        <textarea
          id="cn-msg"
          required
          rows={5}
          minLength={10}
          value={message}
          onChange={(e) => setMessage(e.target.value)}
          style={{ ...inputStyle, borderRadius: 12 }}
        />
      </div>
      <label
        style={{
          display: "flex",
          gap: 8,
          alignItems: "flex-start",
          fontSize: 13,
          color: "var(--color-ink-muted)",
        }}
      >
        <input
          type="checkbox"
          checked={accepted}
          onChange={(e) => setAccepted(e.target.checked)}
          required
          style={{ marginTop: 4 }}
        />
        <span>
          He leído la{" "}
          <a href="/legal/privacidad" style={{ textDecoration: "underline" }}>
            política de privacidad
          </a>{" "}
          y acepto el tratamiento de mis datos para responder a mi consulta.
        </span>
      </label>
      <button
        type="submit"
        disabled={!canSend}
        style={{
          background: canSend ? "var(--color-ink)" : "var(--color-ink-muted)",
          color: "var(--color-cream)",
          border: "none",
          padding: "0.875rem 1.5rem",
          borderRadius: 9999,
          fontSize: 15,
          fontWeight: 700,
          cursor: canSend ? "pointer" : "not-allowed",
        }}
      >
        Enviar
      </button>
      <p
        style={{
          fontSize: 12,
          color: "var(--color-ink-muted)",
          fontStyle: "italic",
        }}
      >
        Al enviar se abrirá tu app de correo con el mensaje preparado. También
        puedes escribir directamente a {site.email}.
      </p>
    </form>
  );
}
```

- [ ] **Step 2: Write `src/pages/contacto.astro`**

```astro
---
import MarketingLayout from "~/layouts/MarketingLayout.astro";
import ContactForm from "~/islands/ContactForm.tsx";
import { stores } from "~/data/stores";
import { site } from "~/data/site";
---
<MarketingLayout
  title="Contacto"
  description="Escríbenos o llama. Te respondemos en menos de 24 horas."
>
  <section class="mx-auto max-w-6xl px-4 sm:px-6 py-12 grid gap-12 md:grid-cols-2">
    <div>
      <p class="text-[11px] uppercase tracking-[0.25em] text-[color:var(--color-ink-muted)] font-semibold">Contacto</p>
      <h1 class="text-4xl sm:text-5xl mt-2">Hablamos.</h1>
      <p class="text-[color:var(--color-ink-muted)] mt-3 max-w-md">Para pedidos pequeños usa WhatsApp en cada tienda. Para catering, eventos o consultas largas, este formulario.</p>

      <ul class="mt-8 space-y-5 text-sm">
        {stores.map((s) => (
          <li>
            <p class="font-[family-name:var(--font-display)] text-xl">{s.shortName}</p>
            <p class="text-[color:var(--color-ink-muted)]">{s.address}, {s.postal} {s.city}</p>
            <p><a href={`tel:${s.phone}`}>{s.phoneDisplay}</a> · <a href={`mailto:${s.email}`}>{s.email}</a></p>
          </li>
        ))}
        <li>
          <p class="text-[10px] uppercase tracking-[0.25em] text-[color:var(--color-ink-muted)] font-semibold">Redes</p>
          <p class="mt-1"><a href={site.social.instagram} rel="noopener" target="_blank">Instagram</a> · <a href={site.social.facebook} rel="noopener" target="_blank">Facebook</a></p>
        </li>
      </ul>
    </div>

    <div>
      <ContactForm client:visible />
    </div>
  </section>
</MarketingLayout>
```

- [ ] **Step 3: Commit**

```bash
git add src/islands/ContactForm.tsx src/pages/contacto.astro
git commit -m "feat(pages): contacto with mailto-based form + store info"
```

---

### Task 28: Legal pages

**Files:**

- Create: `src/pages/legal/aviso-legal.astro`
- Create: `src/pages/legal/privacidad.astro`
- Create: `src/pages/legal/cookies.astro`

- [ ] **Step 1: Write `src/pages/legal/aviso-legal.astro`**

```astro
---
import LegalLayout from "~/layouts/LegalLayout.astro";
import { site } from "~/data/site";
---
<LegalLayout title="Aviso legal" description="Información legal del sitio web de Horno San Lorenzo.">
  <p>
    En cumplimiento de la Ley 34/2002 de Servicios de la Sociedad de la Información y de Comercio
    Electrónico (LSSI-CE), se informa a los usuarios de los siguientes datos del titular del sitio web:
  </p>
  <h2>Titular</h2>
  <ul>
    <li><strong>Denominación social:</strong> Horno San Lorenzo (razón social a completar).</li>
    <li><strong>CIF:</strong> (pendiente de proporcionar por el cliente).</li>
    <li><strong>Domicilio:</strong> C/ Valgrande 21, Nave 2-k, 28108 Alcobendas (Madrid).</li>
    <li><strong>Email:</strong> <a href={`mailto:${site.email}`}>{site.email}</a></li>
    <li><strong>Sitio web:</strong> {site.url}</li>
  </ul>
  <h2>Condiciones de uso</h2>
  <p>
    El acceso a esta web es gratuito. El usuario se compromete a hacer un uso adecuado de los contenidos y servicios
    que el titular ofrece y a no emplearlos para incurrir en actividades ilícitas o lesivas de terechos de terceros.
  </p>
  <h2>Propiedad intelectual</h2>
  <p>
    Todos los contenidos del sitio (textos, imágenes, diseños, código) son propiedad del titular o cuentan con la
    autorización correspondiente para su uso. Queda prohibida su reproducción sin autorización previa.
  </p>
  <h2>Modificaciones</h2>
  <p>
    El titular se reserva el derecho a modificar cualquier información que aparezca en el sitio sin previo aviso.
  </p>
</LegalLayout>
```

- [ ] **Step 2: Write `src/pages/legal/privacidad.astro`**

```astro
---
import LegalLayout from "~/layouts/LegalLayout.astro";
import { site } from "~/data/site";
---
<LegalLayout title="Política de privacidad" description="Cómo tratamos tus datos cuando contactas con nosotros.">
  <p>
    Esta política regula el tratamiento de datos personales que se realiza a través del formulario de contacto del sitio.
  </p>
  <h2>Responsable del tratamiento</h2>
  <p>Horno San Lorenzo (datos completos en el <a href="/legal/aviso-legal">aviso legal</a>).</p>
  <h2>Datos que recogemos</h2>
  <ul>
    <li>Nombre y apellidos.</li>
    <li>Email.</li>
    <li>Teléfono (opcional).</li>
    <li>Contenido del mensaje.</li>
  </ul>
  <h2>Finalidad</h2>
  <p>Responder a tu consulta. No usamos tus datos para marketing ni los cedemos a terceros.</p>
  <h2>Base legal</h2>
  <p>Consentimiento expreso al marcar la casilla del formulario.</p>
  <h2>Plazo de conservación</h2>
  <p>Hasta la resolución de la consulta y, posteriormente, durante el plazo legal de prescripción de obligaciones.</p>
  <h2>Tus derechos</h2>
  <p>
    Puedes ejercer tus derechos de acceso, rectificación, supresión, oposición, limitación y portabilidad escribiendo a
    <a href={`mailto:${site.email}`}>{site.email}</a>. También puedes reclamar ante la Agencia Española de Protección de Datos.
  </p>
</LegalLayout>
```

- [ ] **Step 3: Write `src/pages/legal/cookies.astro`**

```astro
---
import LegalLayout from "~/layouts/LegalLayout.astro";
---
<LegalLayout title="Política de cookies" description="Qué cookies usa este sitio y cómo gestionarlas.">
  <p>
    Este sitio utiliza un número mínimo de cookies y de almacenamiento local (localStorage), todas con finalidad técnica
    o de mejora de la experiencia. Detallamos a continuación qué se guarda y por qué.
  </p>
  <h2>Cookies técnicas (estrictamente necesarias)</h2>
  <ul>
    <li><strong>hsl-cart-v1</strong> — almacena tu carrito en localStorage para que no se pierda al recargar.</li>
    <li><strong>hsl-cookie-consent</strong> — guarda tu decisión sobre cookies para no volver a preguntarte.</li>
  </ul>
  <p>Estas cookies no requieren consentimiento porque son imprescindibles para el funcionamiento.</p>
  <h2>Analítica anónima (sin cookies)</h2>
  <p>
    Usamos Vercel Analytics, que mide visitas de forma agregada y anónima, sin cookies y sin almacenar identificadores
    personales. No requiere consentimiento.
  </p>
  <h2>Cookies de terceros (con consentimiento)</h2>
  <ul>
    <li><strong>Google Maps</strong> — al embeber el mapa de las tiendas, Google puede instalar cookies. Solo cargamos el mapa si has aceptado.</li>
  </ul>
  <p>
    Si rechazas las cookies, mostramos un enlace al mapa en Google Maps en lugar del embed. Puedes cambiar tu decisión en
    cualquier momento borrando el almacenamiento local del sitio en tu navegador.
  </p>
</LegalLayout>
```

- [ ] **Step 4: Commit**

```bash
git add src/pages/legal/
git commit -m "feat(pages): aviso legal, privacidad, cookies"
```

---

## Phase 8 — Ship

### Task 29: Static assets (images, favicon, robots, OG)

**Files:**

- Create: `public/favicon.svg`
- Create: `public/robots.txt`
- Create: `public/og-image.jpg`
- Create: `public/images/hero/hero-pan.jpg`
- Create: `public/images/productos/*.jpg` (one per product, 23 files)
- Create: `public/images/noticias/*.jpg` (one per noticia, 5 files)
- Create: `public/images/tiendas/*.jpg` (alcobendas.jpg, pozuelo.jpg)

- [ ] **Step 1: Write `public/favicon.svg`**

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <rect width="64" height="64" rx="12" fill="#2b1d12"/>
  <text x="50%" y="58%" text-anchor="middle" fill="#f4ecdf" font-family="Georgia, serif" font-style="italic" font-weight="700" font-size="34">H</text>
</svg>
```

- [ ] **Step 2: Write `public/robots.txt`**

```
User-agent: *
Allow: /

Sitemap: https://hornosanlorenzo-demo.vercel.app/sitemap-index.xml
```

- [ ] **Step 3: Download Unsplash images**

For each image slot below, search [unsplash.com](https://unsplash.com) for the keyword, pick a vertical-friendly photo, download as JPG at ≥1200px on the long side, and save to the given path. Filenames must exactly match the slugs used in the markdown frontmatter.

Hero:

- `public/images/hero/hero-pan.jpg` — search: "sourdough bread crust"

Productos (must match slugs in `src/content/products/*.md`):
| File | Search term |
|------|-------------|
| `pan-de-pueblo.jpg` | rustic bread loaf |
| `pan-integral.jpg` | whole wheat bread |
| `pan-candeal.jpg` | white country bread |
| `hogaza-rustica.jpg` | sourdough boule |
| `pan-de-cereales.jpg` | seeded bread |
| `baguette.jpg` | french baguette |
| `croissant-mantequilla.jpg` | butter croissant |
| `napolitana-chocolate.jpg` | pain au chocolat |
| `ensaimada.jpg` | ensaimada mallorca |
| `palmera-chocolate.jpg` | chocolate palmier |
| `suizo.jpg` | sweet bread bun sugar |
| `magdalena-casera.jpg` | spanish magdalena muffin |
| `tarta-de-zanahoria.jpg` | carrot cake slice |
| `tarta-de-queso.jpg` | basque cheesecake |
| `sacher.jpg` | sacher torte |
| `tarta-de-manzana.jpg` | apple tart french |
| `tarta-tres-chocolates.jpg` | chocolate layered cake |
| `empanada-gallega.jpg` | galician empanada |
| `quiche-lorraine.jpg` | quiche lorraine |
| `hojaldre-jamon-y-queso.jpg` | ham cheese pastry |
| `roscon-de-reyes.jpg` | spanish kings cake roscon |
| `torrijas.jpg` | torrijas spanish toast |
| `panettone.jpg` | panettone sliced |

Noticias:

- `roscon-2026.jpg` — kings cake nata
- `san-valentin.jpg` — chocolate heart cake
- `dia-del-padre.jpg` — pastry tie
- `san-lorenzo.jpg` — olive oil torta spanish
- `navidad.jpg` — spanish christmas pastries

Tiendas:

- `alcobendas.jpg` — artisan bakery storefront warm light
- `pozuelo.jpg` — pastry shop window display

OG image:

- `public/og-image.jpg` — same as hero or a wider variant, 1200×630

License: Unsplash photos are royalty-free under the Unsplash License. Keep a `public/images/CREDITS.txt` with photographer attributions if known. Not legally required but courteous.

- [ ] **Step 4: Verify production build**

Run: `pnpm build`
Expected: build completes, `dist/` has all pages (`index.html`, `catalogo/*`, `noticias/*`, `tiendas/index.html`, etc.). No "missing image" errors.

- [ ] **Step 5: Verify dev server**

Run: `pnpm dev` and visit:

- `http://localhost:4321/` — home loads with hero
- `http://localhost:4321/catalogo` — grid of products with photos
- `http://localhost:4321/catalogo/pan-de-pueblo` — detail page works
- `http://localhost:4321/carrito` — empty state
- Add a product from `/catalogo/pan-de-pueblo`, then visit `/carrito` — pickup form appears, WhatsApp button enabled after selecting day
- Click the WhatsApp button → URL contains the formatted message

- [ ] **Step 6: Commit**

```bash
git add public/
git commit -m "chore(assets): favicon, robots, og, hero+products+noticias+tiendas images"
```

---

### Task 30: README, Vercel deployment

**Files:**

- Create: `README.md`

- [ ] **Step 1: Write `README.md`**

````markdown
# Horno San Lorenzo — demo

Demo de pitch para presentar a Horno San Lorenzo (Alcobendas + Pozuelo) un rediseño moderno
de su sitio web actual, con flujo de pedidos por WhatsApp sin backend.

## Stack

- Astro 5 (`output: 'static'`)
- React 19 (solo islas: carrito, badge, filtros, contacto, cookies, toast)
- Tailwind v4 (vía `@tailwindcss/vite`, sin config file)
- TypeScript estricto
- Fraunces Variable + Inter Variable (self-hosted via `@fontsource-variable/*`)
- Astro Content Collections + Zod schemas
- Deploy: Vercel Hobby (estático)

## Requisitos

- Node 24 (`.nvmrc`)
- pnpm 10

## Desarrollo

```bash
pnpm install
cp .env.example .env       # rellena PUBLIC_WHATSAPP_NUMBER con tu número
pnpm dev                    # http://localhost:4321
```

## Comandos

```bash
pnpm dev       # servidor de desarrollo
pnpm build     # build estático en dist/
pnpm preview   # sirve dist/ localmente
pnpm check     # astro check (typescript + content)
```

## Despliegue (Vercel)

```bash
pnpm dlx vercel --prod
```

Variables de entorno necesarias en Vercel:

- `PUBLIC_WHATSAPP_NUMBER` — número en formato `wa.me` (sin `+`), ej. `34666123456`
- `PUBLIC_SITE_URL` — URL canónica (`https://hornosanlorenzo-demo.vercel.app`)

> Para el pitch, el `PUBLIC_WHATSAPP_NUMBER` apunta al móvil de Óscar. Cuando el cliente firme,
> se sustituye por el suyo. Ver el spec para el porqué.

## Estructura

```
src/
├── content/{products,noticias}/*.md  ← Content Collections
├── data/                              ← site, stores, services, categorías, copy
├── lib/{cart,whatsapp,format}.ts      ← lógica pura
├── hooks/useCart.ts                   ← hook React del carrito
├── layouts/                           ← BaseLayout, MarketingLayout, LegalLayout
├── components/                        ← Astro components (server)
├── islands/                           ← React components (client)
├── styles/global.css                  ← Tailwind v4 + @theme tokens
└── pages/                             ← rutas
```

## Documentación

- Spec: [`docs/superpowers/specs/2026-05-19-hornosanlorenzo-demo-design.md`](docs/superpowers/specs/2026-05-19-hornosanlorenzo-demo-design.md)
- Plan: [`docs/superpowers/plans/2026-05-19-hornosanlorenzo-demo.md`](docs/superpowers/plans/2026-05-19-hornosanlorenzo-demo.md)
````

- [ ] **Step 2: Initial Vercel deploy**

Run from project root:

```bash
pnpm dlx vercel --prod
```

When prompted:

- Set up and deploy? **Y**
- Scope? Choose `oscarsr96s-projects`
- Link to existing project? **N**
- Project name? `hornosanlorenzo-demo`
- Directory? `./` (default)
- Override settings? **N**

After first deploy, set env vars from the Vercel dashboard (Project → Settings → Environment Variables):

- `PUBLIC_WHATSAPP_NUMBER` = your real number (no `+`)
- `PUBLIC_SITE_URL` = the production URL (e.g. `https://hornosanlorenzo-demo.vercel.app`)

Trigger a fresh production deploy so the new env vars take effect:

```bash
pnpm dlx vercel --prod
```

Verify:

- Open the production URL on a mobile browser.
- Add a product, complete the pickup form, tap "Pedir por WhatsApp".
- Confirm WhatsApp opens with the formatted message on the configured number.

- [ ] **Step 3: Commit README**

```bash
git add README.md
git commit -m "docs: README with stack, dev, and deploy instructions"
```

- [ ] **Step 4: Push to GitHub**

Create empty repo `oscarsr96/hornosanlorenzo-demo` (private) at https://github.com/new, then:

```bash
git remote add origin git@github.com:oscarsr96/hornosanlorenzo-demo.git
git branch -M main
git push -u origin main
```

Connect the GitHub repo to Vercel (Project Settings → Git) so future pushes auto-deploy.

---

### Task 31: Final QA — Lighthouse, axe, mobile flow

**Files:** (no code changes; fixes addressed inline per finding)

- [ ] **Step 1: Lighthouse mobile audit on home**

Run:

```bash
pnpm build && pnpm preview
```

Open Chrome → DevTools → Lighthouse → Mobile + Performance/Accessibility/Best Practices/SEO → Analyze on `http://localhost:4321`.

Expected:

- Performance ≥ 95
- Accessibility = 100
- Best Practices = 100
- SEO = 100

If anything is below target: investigate failing audit, fix in code (most likely candidates: missing image dimensions, missing alt text, contrast on a specific element).

- [ ] **Step 2: Lighthouse mobile on /carrito (with items)**

Add 3 items via /catalogo, then run Lighthouse on `/carrito`.

Expected:

- Performance ≥ 90 (slightly lower acceptable due to React island)
- Accessibility = 100

- [ ] **Step 3: axe-core scan**

In DevTools → Lighthouse → Accessibility, or install the [axe DevTools extension](https://chromewebstore.google.com/detail/axe-devtools-web-accessib) and run on each main page:

- /
- /catalogo
- /catalogo/pan-de-pueblo
- /carrito (empty + with items)
- /tiendas
- /contacto
- /noticias
- /noticias/roscon-reyes-2026

Expected: 0 critical/serious findings. Fix any that appear inline.

- [ ] **Step 4: Mobile flow on real device**

On your phone (iOS Safari + Android Chrome if available), visit the production URL:

1. Browse `/catalogo`, filter by "Tartas".
2. Open `/catalogo/tarta-de-zanahoria`, pick variant "10 personas", qty 1, tap "Añadir".
3. Verify toast appears, header badge updates to "1".
4. Visit `/carrito`. Select Pozuelo, a valid day, "tarde", enter your name.
5. Tap "Pedir por WhatsApp".
6. WhatsApp opens with the formatted message → confirm message arrives on the configured number.

Fix any layout glitches on the way.

- [ ] **Step 5: View transitions sanity check**

Navigate between pages and confirm header/footer persist (no flicker). If they re-mount: ensure `transition:persist` is set on Header/Footer roots.

- [ ] **Step 6: Cookie banner end-to-end**

In a private window:

1. Load home → banner appears.
2. Visit `/tiendas` → map shows placeholder.
3. Tap "Aceptar" → map loads. Reload tiendas → map still loads.
4. Clear localStorage → banner reappears. Tap "Rechazar" → map placeholder persists across navigation.

- [ ] **Step 7: Commit any QA fixes**

```bash
git add -A
git commit -m "fix(qa): address Lighthouse/axe/mobile findings"
git push
```

---

## Self-Review

After all tasks complete, verify against the spec:

- ✅ §5 Routes (11 static + 2 dynamic) → Tasks 21–28
- ✅ §6 Stack (Astro 5 + React + Tailwind v4 + motion + zod) → Task 1 deps + Task 3 CSS
- ✅ §7 Cart + WhatsApp flow → Tasks 9, 10, 11, 24
- ✅ §8 Visual Warm Artisan tokens + Fraunces/Inter → Task 3
- ✅ §9 Data model (site/stores/services/categories/products/noticias) → Tasks 4, 5, 6, 7
- ✅ §10 Astro/React component split + 7 islands → Tasks 13–18, 19, 22, 24, 27
- ✅ §11 SEO + JSON-LD (Org, LocalBusiness, Product, Article, Breadcrumb) → Tasks 12, 16, 21, 23, 25, 26
- ✅ §12 Performance (Lighthouse ≥95 targets) → Task 31
- ✅ §13 Accessibility (skip link, lang=es, alt, focus, reduced-motion) → Tasks 3, 15, 16, 31
- ✅ §14 Cookies + LSSI legal pages → Tasks 17, 20, 28
- ✅ §15 Vercel Analytics → Task 16 (BaseLayout)
- ✅ §16 Deployment + env vars → Task 30
- ✅ §17 Repo structure matches file map → all tasks
- ✅ §18 "Done" criteria covered → Task 31

---

## Plan complete and saved to `docs/superpowers/plans/2026-05-19-hornosanlorenzo-demo.md`.

**Two execution options:**

1. **Subagent-Driven (recommended)** — I dispatch a fresh subagent per task, review between tasks, fast iteration.
2. **Inline Execution** — Execute tasks in this session with checkpoints for review.

**Which approach?**
